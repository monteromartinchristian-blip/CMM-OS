# Phase 10.48 Remediation V1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remediate exactly the two MAJOR and two MINOR findings from Phase 10.48 Independent Audit V1 without reopening the verified Domain Quality Metrics architecture.

**Architecture:** Keep `DomainDefinition.quality_metrics`, the quality contracts, the twelve first-party catalogs, Phase 10.47 benchmark semantics, observability isolation, and the Phase 11 execution boundary unchanged. Apply one minimal public-deserialization fix, strengthen Decimal and traceability regression evidence, and add an audit-only baseline-aware Ruff/format verifier that proves Phase 10.48 introduced no new legacy lint/format debt without falsely claiming repository-wide PASS.

**Tech Stack:** Python >=3.10, dataclasses, `Decimal`, `pytest>=9,<10`, `ruff>=0.9,<1`, Git, `git archive`, Markdown documentation.

**Spec:** `docs/superpowers/specs/2026-09-10-phase-10.48-remediation-v1-design.md`

**Source audit:** `docs/audits/phase-10.48-independent-audit-v1.md`

**Plan authoring branch:** `feature/phase-10-domain-intelligence`

**Plan authoring HEAD:** `b8401642af5e40a52ab0c9d1ae91e93430267e5f`

**Pre-Phase-10.48 implementation baseline:** `35bf9d2b33c9e3c31ecd789c5d1237590eb31816`

## Global Constraints

- Remediate only `MAJOR-01`, `MAJOR-02`, `MINOR-01`, and `MINOR-02` from Independent Audit V1.
- Preserve the original Phase 10.48 design, original implementation plan, and Audit V1 as immutable historical artifacts.
- Do not modify first-party quality metric IDs, weights, thresholds, blocking flags, evaluator IDs, or catalog membership.
- Do not change `DomainDefinition.quality_metrics` ownership or the canonical Domain Pack path.
- Do not change Phase 10.47 `DomainBenchmarkCase` semantics.
- Do not repurpose `DomainMetricMeasurement`, `DomainMetricsSnapshot`, or `DomainMetricsCalculator`.
- Do not add a quality registry, loader, resolver, store, runtime, engine, evaluator runner, benchmark runner, model/provider execution, comparison, ranking, regression engine, or routing implementation.
- Do not change assessment arithmetic unless a new focused regression proves the existing implementation defective.
- Do not implement Phases 10.49–10.53 or Phase 11 behavior.
- Use TDD for every behavioral change: RED → minimal fix → GREEN → controlled refactor.
- Do not mass-format or mass-lint-fix unrelated repository files.
- Whole-repository Ruff/format legacy debt must be reported truthfully; never label a non-zero whole-repository run as PASS.
- Baseline-aware Ruff/format verification is specific to this remediation and does not globally redefine CMM OS gate policy.
- Use the exact Ruff executable from the repository virtual environment for baseline and current comparisons.
- Extract the historical baseline only with `git archive`; do not use `git worktree`.
- Preserve the quarantine stash.
- Never run `git stash`, `git stash pop`, `git stash apply`, `git stash drop`, `git reset`, `git clean`, or `git worktree`.
- Do not push or merge.
- Every implementation commit must have coherent, narrow scope.
- Before a new audit bundle: all remediation changes are committed, the worktree is clean, the quarantine stash is unchanged, and the bundle comes from exact committed HEAD.
- The implementation-agent prompt generated after this plan is committed supplies the exact implementation starting HEAD. The executor must assert that HEAD before touching code.

---

## File Structure

### Production code

- Modify: `cmm/domains/quality_contracts.py`
  - Remove premature coercion of human-review `notes` in `DomainQualityHumanReviewResult.from_dict()`.
  - Preserve all other quality contract behavior.

### Focused tests

- Modify: `tests/domains/test_domain_quality_contracts.py`
  - Add adversarial malformed-`notes` deserialization tests.
  - Correct the false non-unit-weight test.
  - Add deterministic non-terminating-ratio evidence without redesigning arithmetic.

- Modify: `tests/domains/test_domain_quality_dp048_acceptance.py`
  - Strengthen the existing connected fail-closed acceptance with malformed nested human-review evidence.

- Create: `tests/domains/test_domain_prompt_clause_coverage.py`
  - Parse the canonical clause coverage ledger.
  - Require unique clause IDs, `R10-C48` exactly once, and totals derived from actual rows.

- Create: `tests/domains/test_phase_10_48_remediation_gates.py`
  - Unit-test the pure baseline-aware gate decision logic.
  - Guard the baseline constant and safety invariants of the audit verifier.

### Audit tooling

- Create: `scripts/audit/verify_phase_10_48_ruff_baseline.py`
  - Extract pre-10.48 HEAD with `git archive` into a temporary directory.
  - Run one Ruff version against baseline and current repo.
  - Measure global Ruff diagnostic count and global format-debt file count.
  - Compute changed Python files from baseline to current working tree, including untracked Python files.
  - Require all changed Python files to pass Ruff and format checks.
  - Require current global debt not to exceed baseline global debt.
  - Print machine-readable `KEY=VALUE` evidence.
  - Never mutate Git state.

### Documentation

- Modify: `docs/audits/domain-prompt-clause-coverage.md`
  - Add `R10-C48`.
  - Correct derived coverage totals.

- Modify: `docs/reference/domain-quality-metrics.md`
  - Correct non-terminating Decimal ratio wording.
  - Document the remediation's truthful baseline-aware lint/format evidence boundary if appropriate.

- Modify: `docs/reference/domain-intelligence-requirements-matrix.md`
  - Record Audit V1 FAIL and remediation-reported state without claiming independent verification.

- Modify: `docs/roadmap/phase-10-domain-intelligence.md`
  - Record current remediation/re-audit status only.
  - Preserve historical Phase 10.47 closure evidence.

- Modify: `ROADMAP.md`
  - Record current Phase 10.48 status as pending Independent Re-audit V2.

### Historical artifacts that must not change

- Do not modify: `docs/audits/phase-10.48-independent-audit-v1.md`
- Do not modify: `docs/superpowers/specs/2026-09-10-phase-10.48-domain-quality-metrics-design.md`
- Do not modify: `docs/superpowers/plans/2026-09-10-phase-10.48-domain-quality-metrics-implementation-plan.md`
- Do not modify: `docs/superpowers/specs/2026-09-10-phase-10.48-remediation-v1-design.md`

---

## Execution Preflight

Before Task 1, the implementation agent must run the exact preflight from its implementation prompt and additionally verify:

```bash
set -euo pipefail

git branch --show-current
git rev-parse HEAD
git status --short
git stash list
git log -8 --oneline --decorate

test -z "$(git status --porcelain)"
git stash list | grep -F \
  "quarantine: post-audit phase 10.32 uncommitted changes"

test -f docs/audits/phase-10.48-independent-audit-v1.md
test -f docs/superpowers/specs/2026-09-10-phase-10.48-remediation-v1-design.md
test -f docs/superpowers/plans/2026-09-10-phase-10.48-remediation-v1-implementation-plan.md

grep -F 'INDEPENDENT_AUDIT_V1=FAIL' \
  docs/audits/phase-10.48-independent-audit-v1.md

grep -F 'MAJORS=2' \
  docs/audits/phase-10.48-independent-audit-v1.md

grep -F 'MINORS=2' \
  docs/audits/phase-10.48-independent-audit-v1.md
```

Expected:

```text
branch = feature/phase-10-domain-intelligence
worktree = clean
quarantine stash = present and unchanged
Audit V1 = FAIL
MAJORS = 2
MINORS = 2
```

Do not begin Task 1 if any preflight invariant fails.

---

### Task 1: Fail Closed on Malformed Human-Review `notes`

**Findings:** `MAJOR-02`

**Files:**
- Modify: `tests/domains/test_domain_quality_contracts.py`
- Modify: `cmm/domains/quality_contracts.py`

**Interfaces:**
- Consumes: `DomainQualityHumanReviewResult.from_dict(data: Mapping[str, Any])`
- Consumes: existing `_require_str_tuple(value, field_name)` constructor validator
- Consumes: existing `_wrap_validation_error(...)`
- Produces: strict serialized `notes` validation with `DomainSerializationError` for malformed payload shapes
- Preserves: valid `list[str]` / `tuple[str, ...]` deserialization and constructor behavior

- [ ] **Step 1: Add adversarial RED tests for malformed serialized `notes`**

Add this test next to existing human-review round-trip tests:

```python
@pytest.mark.parametrize(
    "notes",
    [
        "abc",
        b"abc",
        bytearray(b"abc"),
        {"a": "x", "b": "y"},
        123,
        True,
        1.25,
        None,
        ["ok", 1],
        [["nested"]],
    ],
)
def test_human_review_from_dict_rejects_malformed_notes_fail_closed(notes):
    payload = DomainQualityHumanReviewResult(
        id="quality-review:health:prudence:malformed",
        schema_version="1",
        status="accepted",
        notes=("valid",),
    ).to_dict()
    payload["notes"] = notes

    with pytest.raises(DomainSerializationError):
        DomainQualityHumanReviewResult.from_dict(payload)
```

Add a valid raw-list control:

```python
def test_human_review_from_dict_accepts_serialized_list_of_notes():
    payload = DomainQualityHumanReviewResult(
        id="quality-review:health:prudence:list",
        schema_version="1",
        status="accepted",
        notes=("one", "two"),
    ).to_dict()

    assert payload["notes"] == ["one", "two"]
    restored = DomainQualityHumanReviewResult.from_dict(payload)
    assert restored.notes == ("one", "two")
```

- [ ] **Step 2: Run only the new tests and prove RED**

Run:

```bash
.venv/bin/python -m pytest \
  tests/domains/test_domain_quality_contracts.py \
  -k 'human_review_from_dict_rejects_malformed_notes_fail_closed or human_review_from_dict_accepts_serialized_list_of_notes' \
  -q
```

Expected before the fix:

- invalid string/mapping payloads are accepted or semantically coerced;
- invalid scalar payloads may leak raw `TypeError`;
- test command exits non-zero.

Record the RED output in the implementation session.

- [ ] **Step 3: Apply the minimal production fix**

In `DomainQualityHumanReviewResult.from_dict()`, replace:

```python
notes=tuple(data.get("notes", ()) or ()),
```

with:

```python
notes=data.get("notes", ()),
```

Do not add a second notes parser.

Why this is sufficient:

```text
raw serialized value
    ↓
DomainQualityHumanReviewResult(...)
    ↓
__post_init__()
    ↓
_require_str_tuple(...)
    ↓
DomainContractValidationError
    ↓
existing from_dict exception wrapper
    ↓
DomainSerializationError
```

Explicit `None` now fails closed rather than being silently treated as an empty tuple.

- [ ] **Step 4: Run the new tests and prove GREEN**

Run:

```bash
.venv/bin/python -m pytest \
  tests/domains/test_domain_quality_contracts.py \
  -k 'human_review_from_dict_rejects_malformed_notes_fail_closed or human_review_from_dict_accepts_serialized_list_of_notes' \
  -q
```

Expected: PASS.

- [ ] **Step 5: Run the complete quality contract test file**

Run:

```bash
.venv/bin/python -m pytest \
  tests/domains/test_domain_quality_contracts.py \
  -q
```

Expected: PASS.

- [ ] **Step 6: Run Ruff and format on Task 1 Python files**

Run:

```bash
.venv/bin/python -m ruff check \
  cmm/domains/quality_contracts.py \
  tests/domains/test_domain_quality_contracts.py

.venv/bin/python -m ruff format --check \
  cmm/domains/quality_contracts.py \
  tests/domains/test_domain_quality_contracts.py
```

Expected: PASS.

- [ ] **Step 7: Verify diff scope**

Run:

```bash
git diff --check
git diff -- \
  cmm/domains/quality_contracts.py \
  tests/domains/test_domain_quality_contracts.py
```

Expected: only malformed-notes tests and the minimal deserialization change.

- [ ] **Step 8: Commit Task 1**

Run:

```bash
git add \
  cmm/domains/quality_contracts.py \
  tests/domains/test_domain_quality_contracts.py

git diff --cached --check
git diff --cached --name-only

git commit -m \
  "fix(domains): fail closed malformed quality review notes"
```

Expected: one narrow remediation commit.

---

### Task 2: Strengthen Connected Acceptance for Nested Human-Review Deserialization

**Findings:** `MAJOR-02`

**Files:**
- Modify: `tests/domains/test_domain_quality_dp048_acceptance.py`

**Interfaces:**
- Consumes: `export_domain_quality_assessment(...)`
- Consumes: `import_domain_quality_assessment(...)`
- Consumes: real Health quality metrics
- Produces: connected acceptance proof that malformed nested human-review `notes` fail through the public assessment deserialization path
- Preserves: existing Scenario A–J structure

- [ ] **Step 1: Add a connected regression under Scenario F or H**

Add a test equivalent to:

```python
def test_scenario_h_malformed_nested_human_review_notes_fail_closed() -> None:
    metrics = build_health_quality_metrics()
    prudence = next(metric for metric in metrics if metric.name == "prudence")

    review = DomainQualityHumanReviewResult(
        id="quality-review:health:prudence:malformed",
        schema_version="1",
        status="accepted",
        notes=("valid",),
    )

    results = tuple(
        build_domain_quality_metric_result(
            metric,
            score=Decimal("0.95"),
            evaluator_version="1",
            confidence=Decimal("0.90"),
            human_review_results=(review,) if metric.id == prudence.id else (),
        )
        for metric in metrics
    )

    assessment = assess_domain_quality(metrics, results)
    payload = assessment.to_dict()

    prudence_payload = next(
        result
        for result in payload["metric_results"]
        if result["metric_id"] == prudence.id
    )
    prudence_payload["human_review_results"][0]["notes"] = "not-a-list"

    with pytest.raises(DomainSerializationError):
        DomainQualityAssessment.from_dict(payload)
```

Use already imported public functions/types where possible. If `DomainSerializationError` or `DomainQualityAssessment` is not already imported in this test module, add only those imports.

- [ ] **Step 2: Prove the acceptance test would have caught audited V1**

Temporarily inspect the Task 1 commit or reason from the recorded V1 behavior, but do not revert or mutate history.

Run the new test on current fixed code:

```bash
.venv/bin/python -m pytest \
  tests/domains/test_domain_quality_dp048_acceptance.py \
  -k malformed_nested_human_review_notes_fail_closed \
  -q
```

Expected: PASS on remediated code.

The V1 audit report itself supplies the historical failing evidence; do not alter/revert the committed fix solely to manufacture a second RED after Task 1.

- [ ] **Step 3: Run all AT-DP-048 scenarios**

Run:

```bash
.venv/bin/python -m pytest \
  tests/domains/test_domain_quality_dp048_acceptance.py \
  -q
```

Expected: PASS.

- [ ] **Step 4: Run scoped Ruff/format and diff check**

Run:

```bash
.venv/bin/python -m ruff check \
  tests/domains/test_domain_quality_dp048_acceptance.py

.venv/bin/python -m ruff format --check \
  tests/domains/test_domain_quality_dp048_acceptance.py

git diff --check
```

Expected: PASS.

- [ ] **Step 5: Commit Task 2**

Run:

```bash
git add tests/domains/test_domain_quality_dp048_acceptance.py

git diff --cached --check
git diff --cached --name-only

git commit -m \
  "test(domains): strengthen quality review fail-closed acceptance"
```

---

### Task 3: Correct Non-Unit Weight and Non-Terminating Decimal Evidence

**Findings:** `MINOR-02`

**Files:**
- Modify: `tests/domains/test_domain_quality_contracts.py`

**Interfaces:**
- Consumes: existing `assess_domain_quality(...)`
- Produces: real non-unit weight normalization evidence
- Produces: ambient-context-independent non-terminating-ratio evidence
- Preserves: production assessment arithmetic

- [ ] **Step 1: Replace the ineffective unit-sum test with genuine non-unit weights**

Replace the existing body of:

```python
test_assessment_weight_normalization_does_not_require_unit_sum
```

with:

```python
def test_assessment_weight_normalization_does_not_require_unit_sum():
    a = _metric(weight=Decimal("0.1"), minimum_score=Decimal("0.5"))
    b = _usefulness_metric(weight=Decimal("0.3"), minimum_score=Decimal("0.5"))
    results = (_build(a, "1.0"), _build(b, "0.5"))

    values = []
    for precision in (10, 28, 50):
        with localcontext(Context(prec=precision)):
            values.append(assess_domain_quality((a, b), results).aggregate_score)

    assert values == [Decimal("0.625")] * 3
```

This proves the denominator is `0.4`, not `1`.

- [ ] **Step 2: Add a non-terminating weighted-ratio context-independence test**

Add:

```python
def test_non_terminating_weighted_ratio_is_context_independent():
    a = _metric(weight=Decimal("0.1"), minimum_score=Decimal("0"))
    b = _usefulness_metric(weight=Decimal("0.2"), minimum_score=Decimal("0"))
    results = (_build(a, Decimal("0")), _build(b, Decimal("1")))

    aggregates = []
    for precision in (10, 28, 50):
        with localcontext(Context(prec=precision)):
            aggregates.append(
                assess_domain_quality((a, b), results).aggregate_score
            )

    assert aggregates[0] == aggregates[1] == aggregates[2]
    assert Decimal("0.66") < aggregates[0] < Decimal("0.67")
```

Do not assert that the rounded Decimal equals the exact rational `2/3`; the purpose is deterministic bounded representation independent of ambient context.

- [ ] **Step 3: Run the two Decimal tests**

Run:

```bash
.venv/bin/python -m pytest \
  tests/domains/test_domain_quality_contracts.py \
  -k 'weight_normalization_does_not_require_unit_sum or non_terminating_weighted_ratio_is_context_independent' \
  -q
```

Expected: PASS on existing arithmetic.

If either test fails, stop and investigate before changing production arithmetic. Do not redesign the arithmetic merely to satisfy a mistaken expected value.

- [ ] **Step 4: Run complete contract tests and scoped style gates**

Run:

```bash
.venv/bin/python -m pytest \
  tests/domains/test_domain_quality_contracts.py \
  -q

.venv/bin/python -m ruff check \
  tests/domains/test_domain_quality_contracts.py

.venv/bin/python -m ruff format --check \
  tests/domains/test_domain_quality_contracts.py

git diff --check
```

Expected: PASS.

- [ ] **Step 5: Commit Task 3**

Run:

```bash
git add tests/domains/test_domain_quality_contracts.py

git diff --cached --check
git diff --cached --name-only

git commit -m \
  "test(domains): correct quality decimal normalization evidence"
```

---

### Task 4: Restore Canonical `R10-C48` Clause Coverage

**Findings:** `MINOR-01`

**Files:**
- Create: `tests/domains/test_domain_prompt_clause_coverage.py`
- Modify: `docs/audits/domain-prompt-clause-coverage.md`

**Interfaces:**
- Consumes: canonical Markdown ledger sections 3 through 7
- Produces: executable invariant that coverage totals equal actual clause rows
- Produces: exactly one `R10-C48` row mapped to `DP-048`
- Preserves: every existing clause row and historical Phase 10.47 mapping

**Important inspected fact:**

At the post-Audit-V1 repository state, sections 3–7 already contain **154 actual unique clause rows** while the final block still says `153`. Therefore adding `R10-C48` should produce 155 rows if no unrelated row changes occur. The test below derives this dynamically instead of trusting the stale counters.

- [ ] **Step 1: Create a RED ledger-consistency test**

Create `tests/domains/test_domain_prompt_clause_coverage.py`:

```python
from __future__ import annotations

import re
from pathlib import Path


LEDGER = Path("docs/audits/domain-prompt-clause-coverage.md")


def _coverage_body(text: str) -> str:
    return text.split(
        "## 3. Coverage of the fifteen prompts",
        1,
    )[1].split(
        "## 8. Final coverage check",
        1,
    )[0]


def _clause_ids(text: str) -> list[str]:
    ids = re.findall(r"^\| `([^`]+)` \|", _coverage_body(text), re.MULTILINE)
    return [clause_id for clause_id in ids if clause_id != "clause_id"]


def _counter(text: str, name: str) -> int:
    match = re.search(rf"^{name} = (\d+)$", text, re.MULTILINE)
    assert match is not None, f"missing coverage counter: {name}"
    return int(match.group(1))


def test_domain_prompt_clause_coverage_is_complete_and_self_consistent():
    text = LEDGER.read_text(encoding="utf-8")
    clause_ids = _clause_ids(text)

    assert clause_ids.count("R10-C48") == 1
    assert len(clause_ids) == len(set(clause_ids))

    assert _counter(text, "total_clauses") == len(clause_ids)
    assert _counter(text, "covered_clauses") == len(clause_ids)
    assert _counter(text, "unclassified_clauses") == 0
    assert _counter(text, "duplicate_primary_mappings") == 0
```

- [ ] **Step 2: Run the new test and prove RED**

Run:

```bash
.venv/bin/python -m pytest \
  tests/domains/test_domain_prompt_clause_coverage.py \
  -q
```

Expected before ledger remediation: FAIL because `R10-C48` is absent and the current `153` counters do not match the already-present 154 rows.

- [ ] **Step 3: Add exactly one `R10-C48` row**

Immediately after `R10-C47`, add:

```markdown
| `R10-C48` | `SRC-R10` · Phase 10.48 Domain Quality Metrics | `DP-048` | `PRIMARY` | Domain-owned declarative quality metrics define weights, thresholds, blocking semantics and deterministic aggregation of already-produced evidence; evaluator/model/provider execution, comparison, ranking, regression detection and routing remain outside Phase 10.48. |
```

Do not change the `R10-C47` row.

- [ ] **Step 4: Recompute the coverage block from actual rows**

With only the new `R10-C48` row added, sections 3–7 should now contain 155 unique clause rows.

Update:

```text
The counts below cover the 102 prompt clauses and 53 specification, preflight, and roadmap clauses listed above.

total_clauses = 155
covered_clauses = 155
unclassified_clauses = 0
duplicate_primary_mappings = 0
```

Before editing the totals, independently derive the count:

```bash
python3 - <<'PY'
from pathlib import Path
import re

text = Path(
    "docs/audits/domain-prompt-clause-coverage.md"
).read_text(encoding="utf-8")

body = text.split(
    "## 3. Coverage of the fifteen prompts",
    1,
)[1].split(
    "## 8. Final coverage check",
    1,
)[0]

ids = [
    value
    for value in re.findall(r"^\| `([^`]+)` \|", body, re.MULTILINE)
    if value != "clause_id"
]

print(f"CLAUSE_ROWS={len(ids)}")
print(f"UNIQUE_CLAUSE_ROWS={len(set(ids))}")
print(f"R10_C48_COUNT={ids.count('R10-C48')}")

assert len(ids) == 155
assert len(set(ids)) == 155
assert ids.count("R10-C48") == 1
PY
```

Expected:

```text
CLAUSE_ROWS=155
UNIQUE_CLAUSE_ROWS=155
R10_C48_COUNT=1
```

- [ ] **Step 5: Run the ledger regression GREEN**

Run:

```bash
.venv/bin/python -m pytest \
  tests/domains/test_domain_prompt_clause_coverage.py \
  -q
```

Expected: PASS.

- [ ] **Step 6: Verify no historical coverage row was rewritten accidentally**

Run:

```bash
git diff -- \
  docs/audits/domain-prompt-clause-coverage.md
```

Expected diff:

- one new `R10-C48` row;
- `51` → `53` in the descriptive count;
- `153` → `155` for total/covered counters;
- no unrelated clause-row edits.

The `51` → `53` change accounts for the pre-existing uncounted `R10-C47` row plus the new `R10-C48` row.

- [ ] **Step 7: Run style/diff checks**

Run:

```bash
.venv/bin/python -m ruff check \
  tests/domains/test_domain_prompt_clause_coverage.py

.venv/bin/python -m ruff format --check \
  tests/domains/test_domain_prompt_clause_coverage.py

git diff --check
```

Expected: PASS.

- [ ] **Step 8: Commit Task 4**

Run:

```bash
git add \
  tests/domains/test_domain_prompt_clause_coverage.py \
  docs/audits/domain-prompt-clause-coverage.md

git diff --cached --check
git diff --cached --name-only

git commit -m \
  "docs(domains): restore phase 10.48 clause traceability"
```

---

### Task 5: Add a Reproducible Baseline-Aware Ruff/Format Gate

**Findings:** `MAJOR-01`

**Files:**
- Create: `scripts/audit/verify_phase_10_48_ruff_baseline.py`
- Create: `tests/domains/test_phase_10_48_remediation_gates.py`

**Interfaces:**
- Consumes: baseline HEAD `35bf9d2b33c9e3c31ecd789c5d1237590eb31816`
- Consumes: current repository working tree
- Consumes: current repository `.venv/bin/python -m ruff`
- Produces: truthful global baseline/current debt counts
- Produces: strict zero-debt checks for every Python file changed since baseline
- Produces: `BASELINE_AWARE_GATE=PASS` only when no Phase 10.48 Ruff/format regression exists
- Does not mutate: branches, index, worktree, stash, refs

#### Gate data model

Implement in the script:

```python
from dataclasses import dataclass


@dataclass(frozen=True)
class RuffDebtSnapshot:
    lint_violations: int
    format_files: int


@dataclass(frozen=True)
class GateDecision:
    changed_python_ruff_pass: bool
    changed_python_format_pass: bool
    no_new_ruff_regressions: bool
    no_new_format_regressions: bool

    @property
    def passed(self) -> bool:
        return all(
            (
                self.changed_python_ruff_pass,
                self.changed_python_format_pass,
                self.no_new_ruff_regressions,
                self.no_new_format_regressions,
            )
        )


def evaluate_gate(
    *,
    baseline: RuffDebtSnapshot,
    current: RuffDebtSnapshot,
    changed_lint_violations: int,
    changed_format_files: int,
) -> GateDecision:
    return GateDecision(
        changed_python_ruff_pass=changed_lint_violations == 0,
        changed_python_format_pass=changed_format_files == 0,
        no_new_ruff_regressions=current.lint_violations <= baseline.lint_violations,
        no_new_format_regressions=current.format_files <= baseline.format_files,
    )
```

- [ ] **Step 1: Add RED unit tests for gate truthfulness**

Create `tests/domains/test_phase_10_48_remediation_gates.py`:

```python
from __future__ import annotations

from pathlib import Path

from scripts.audit.verify_phase_10_48_ruff_baseline import (
    BASELINE_HEAD,
    RuffDebtSnapshot,
    evaluate_gate,
)


def test_phase_10_48_baseline_head_is_pinned():
    assert BASELINE_HEAD == "35bf9d2b33c9e3c31ecd789c5d1237590eb31816"


def test_baseline_gate_passes_only_with_clean_changed_python_and_no_debt_growth():
    decision = evaluate_gate(
        baseline=RuffDebtSnapshot(lint_violations=839, format_files=340),
        current=RuffDebtSnapshot(lint_violations=839, format_files=340),
        changed_lint_violations=0,
        changed_format_files=0,
    )

    assert decision.passed is True


def test_baseline_gate_fails_if_changed_python_has_lint_debt():
    decision = evaluate_gate(
        baseline=RuffDebtSnapshot(lint_violations=839, format_files=340),
        current=RuffDebtSnapshot(lint_violations=839, format_files=340),
        changed_lint_violations=1,
        changed_format_files=0,
    )

    assert decision.passed is False
    assert decision.changed_python_ruff_pass is False


def test_baseline_gate_fails_if_changed_python_has_format_debt():
    decision = evaluate_gate(
        baseline=RuffDebtSnapshot(lint_violations=839, format_files=340),
        current=RuffDebtSnapshot(lint_violations=839, format_files=340),
        changed_lint_violations=0,
        changed_format_files=1,
    )

    assert decision.passed is False
    assert decision.changed_python_format_pass is False


def test_baseline_gate_fails_if_global_lint_debt_grows():
    decision = evaluate_gate(
        baseline=RuffDebtSnapshot(lint_violations=839, format_files=340),
        current=RuffDebtSnapshot(lint_violations=840, format_files=340),
        changed_lint_violations=0,
        changed_format_files=0,
    )

    assert decision.passed is False
    assert decision.no_new_ruff_regressions is False


def test_baseline_gate_fails_if_global_format_debt_grows():
    decision = evaluate_gate(
        baseline=RuffDebtSnapshot(lint_violations=839, format_files=340),
        current=RuffDebtSnapshot(lint_violations=839, format_files=341),
        changed_lint_violations=0,
        changed_format_files=0,
    )

    assert decision.passed is False
    assert decision.no_new_format_regressions is False


def test_baseline_verifier_source_uses_archive_and_no_mutating_git_commands():
    source = Path(
        "scripts/audit/verify_phase_10_48_ruff_baseline.py"
    ).read_text(encoding="utf-8")

    assert "git archive" in source or '"archive"' in source

    for forbidden in (
        "git worktree",
        '"worktree"',
        "git stash",
        '"stash"',
        "git reset",
        '"reset"',
        "git clean",
        '"clean"',
    ):
        assert forbidden not in source
```

- [ ] **Step 2: Run the tests and prove RED**

Run:

```bash
.venv/bin/python -m pytest \
  tests/domains/test_phase_10_48_remediation_gates.py \
  -q
```

Expected: FAIL because the audit verifier module does not exist yet.

- [ ] **Step 3: Create the audit verifier with pure gate logic**

Create `scripts/audit/verify_phase_10_48_ruff_baseline.py`.

Required constants/imports:

```python
from __future__ import annotations

import argparse
import json
import subprocess
import sys
import tarfile
import tempfile
from dataclasses import dataclass
from io import BytesIO
from pathlib import Path


BASELINE_HEAD = "35bf9d2b33c9e3c31ecd789c5d1237590eb31816"
```

Implement the dataclasses and `evaluate_gate(...)` exactly as defined above.

- [ ] **Step 4: Implement safe subprocess helpers**

Use list-form subprocess arguments only.

Core helper:

```python
def _run(
    args: list[str],
    *,
    cwd: Path,
    check: bool = False,
) -> subprocess.CompletedProcess[str]:
    return subprocess.run(
        args,
        cwd=cwd,
        check=check,
        text=True,
        capture_output=True,
    )
```

Do not use `shell=True`.

- [ ] **Step 5: Implement baseline extraction using `git archive` only**

Implement:

```python
def _extract_baseline(repo: Path, destination: Path, baseline: str) -> None:
    archive = subprocess.run(
        ["git", "archive", "--format=tar", baseline],
        cwd=repo,
        check=True,
        capture_output=True,
    ).stdout

    with tarfile.open(fileobj=BytesIO(archive), mode="r:") as tar:
        tar.extractall(destination)
```

Before extraction, resolve the baseline object:

```python
_run(
    ["git", "rev-parse", "--verify", f"{baseline}^{{commit}}"],
    cwd=repo,
    check=True,
)
```

No worktree creation is permitted.

- [ ] **Step 6: Implement Ruff version and lint diagnostic counting**

The same Python/Ruff executable must be used for baseline and current.

Resolve:

```python
ruff_python = repo / ".venv" / "bin" / "python"
```

Require it exists.

Version:

```python
version = _run(
    [str(ruff_python), "-m", "ruff", "--version"],
    cwd=repo,
    check=True,
).stdout.strip()
```

Count lint diagnostics:

```python
def _ruff_lint_count(ruff_python: Path, root: Path) -> tuple[int, int]:
    proc = _run(
        [
            str(ruff_python),
            "-m",
            "ruff",
            "check",
            "--output-format",
            "json",
            ".",
        ],
        cwd=root,
    )
    payload = json.loads(proc.stdout or "[]")
    return proc.returncode, len(payload)
```

Accept Ruff return code `0` or `1`; any other code is an execution error.

- [ ] **Step 7: Implement global format-debt file counting without parsing human prose**

Use tracked/extracted Python files and check each file independently.

For baseline:

```python
def _baseline_python_files(root: Path) -> list[Path]:
    return sorted(path for path in root.rglob("*.py") if path.is_file())
```

For current tracked Python files:

```python
def _tracked_python_files(repo: Path) -> list[Path]:
    proc = _run(
        ["git", "ls-files", "--", "*.py"],
        cwd=repo,
        check=True,
    )
    return [
        repo / line
        for line in proc.stdout.splitlines()
        if line and (repo / line).is_file()
    ]
```

Check formatting per file:

```python
def _format_debt_count(
    ruff_python: Path,
    *,
    root: Path,
    files: list[Path],
) -> int:
    debt = 0
    for path in files:
        relative = path.relative_to(root)
        proc = _run(
            [
                str(ruff_python),
                "-m",
                "ruff",
                "format",
                "--check",
                str(relative),
            ],
            cwd=root,
        )
        if proc.returncode == 1:
            debt += 1
        elif proc.returncode != 0:
            raise RuntimeError(
                f"ruff format failed for {relative}: "
                f"{proc.stdout}{proc.stderr}"
            )
    return debt
```

Also run the actual global command once for truth:

```python
def _global_format_exit(ruff_python: Path, root: Path) -> int:
    proc = _run(
        [str(ruff_python), "-m", "ruff", "format", "--check", "."],
        cwd=root,
    )
    if proc.returncode not in (0, 1):
        raise RuntimeError(proc.stdout + proc.stderr)
    return proc.returncode
```

- [ ] **Step 8: Implement changed-Python discovery including untracked files**

Use the working tree, not only HEAD, so the verifier can run before the final remediation commit.

```python
def _changed_python_files(repo: Path, baseline: str) -> list[Path]:
    changed = _run(
        [
            "git",
            "diff",
            "--name-only",
            "--diff-filter=ACMRT",
            baseline,
            "--",
            "*.py",
        ],
        cwd=repo,
        check=True,
    ).stdout.splitlines()

    untracked = _run(
        [
            "git",
            "ls-files",
            "--others",
            "--exclude-standard",
            "--",
            "*.py",
        ],
        cwd=repo,
        check=True,
    ).stdout.splitlines()

    paths = {
        repo / name
        for name in (*changed, *untracked)
        if name and (repo / name).is_file()
    }
    return sorted(paths)
```

- [ ] **Step 9: Implement changed-file Ruff checks**

Count lint diagnostics only over changed Python files:

```python
def _changed_lint_count(
    ruff_python: Path,
    *,
    repo: Path,
    files: list[Path],
) -> int:
    if not files:
        return 0

    args = [
        str(ruff_python),
        "-m",
        "ruff",
        "check",
        "--output-format",
        "json",
        *[str(path.relative_to(repo)) for path in files],
    ]
    proc = _run(args, cwd=repo)
    if proc.returncode not in (0, 1):
        raise RuntimeError(proc.stdout + proc.stderr)
    return len(json.loads(proc.stdout or "[]"))
```

Use `_format_debt_count(...)` over the changed file list for changed-format debt.

- [ ] **Step 10: Implement the main baseline/current comparison**

Within `TemporaryDirectory()`:

```text
extract baseline with git archive
measure baseline global lint diagnostics
measure baseline format debt
measure current global lint diagnostics
measure current format debt
discover changed Python files
measure changed-file lint/format debt
evaluate gate
print machine-readable evidence
exit 0 only when GateDecision.passed
```

Required output keys:

```text
RUFF_VERSION=
BASELINE_HEAD=

GLOBAL_RUFF_BASELINE_EXIT=
GLOBAL_RUFF_BASELINE=
GLOBAL_RUFF_CURRENT_EXIT=
GLOBAL_RUFF_CURRENT=

GLOBAL_FORMAT_BASELINE_EXIT=
GLOBAL_FORMAT_BASELINE=
GLOBAL_FORMAT_CURRENT_EXIT=
GLOBAL_FORMAT_CURRENT=

CHANGED_PYTHON_FILES=
CHANGED_PYTHON_RUFF_VIOLATIONS=
CHANGED_PYTHON_FORMAT_FILES=

CHANGED_PYTHON_RUFF=PASS|FAIL
CHANGED_PYTHON_FORMAT=PASS|FAIL
NO_NEW_RUFF_REGRESSIONS=PASS|FAIL
NO_NEW_FORMAT_REGRESSIONS=PASS|FAIL

LEGACY_RUFF_DEBT=PRESERVED_OR_REDUCED|INCREASED
LEGACY_FORMAT_DEBT=PRESERVED_OR_REDUCED|INCREASED
BASELINE_AWARE_GATE=PASS|FAIL
```

Print the changed Python paths below a marker:

```text
CHANGED_PYTHON_PATHS_BEGIN
...
CHANGED_PYTHON_PATHS_END
```

Never print `GLOBAL_RUFF=PASS` or `GLOBAL_FORMAT=PASS` unless the corresponding current global count is actually zero.

- [ ] **Step 11: Add CLI parsing**

Required CLI:

```bash
.venv/bin/python \
  scripts/audit/verify_phase_10_48_ruff_baseline.py
```

Optional args:

```text
--repo <path>
--baseline <commit>
```

Defaults:

- repo: Git repository root inferred from script location;
- baseline: exact `BASELINE_HEAD`.

Do not implement mutation flags.

- [ ] **Step 12: Run unit tests GREEN**

Run:

```bash
.venv/bin/python -m pytest \
  tests/domains/test_phase_10_48_remediation_gates.py \
  -q
```

Expected: PASS.

- [ ] **Step 13: Execute the real baseline-aware gate**

Run:

```bash
.venv/bin/python \
  scripts/audit/verify_phase_10_48_ruff_baseline.py \
  | tee "$HOME/Library/Mobile Documents/com~apple~CloudDocs/Downloads/phase-10.48-remediation-v1-ruff-baseline.txt"
```

Expected required truths:

```text
CHANGED_PYTHON_RUFF_VIOLATIONS=0
CHANGED_PYTHON_FORMAT_FILES=0
CHANGED_PYTHON_RUFF=PASS
CHANGED_PYTHON_FORMAT=PASS
NO_NEW_RUFF_REGRESSIONS=PASS
NO_NEW_FORMAT_REGRESSIONS=PASS
LEGACY_RUFF_DEBT=PRESERVED_OR_REDUCED
LEGACY_FORMAT_DEBT=PRESERVED_OR_REDUCED
BASELINE_AWARE_GATE=PASS
```

The exact baseline/current global counts are measured, not assumed.

If `GLOBAL_RUFF_CURRENT` or `GLOBAL_FORMAT_CURRENT` is non-zero, preserve that fact. Do not relabel it PASS.

- [ ] **Step 14: Run scoped style gates over the verifier/tests**

Run:

```bash
.venv/bin/python -m ruff check \
  scripts/audit/verify_phase_10_48_ruff_baseline.py \
  tests/domains/test_phase_10_48_remediation_gates.py

.venv/bin/python -m ruff format --check \
  scripts/audit/verify_phase_10_48_ruff_baseline.py \
  tests/domains/test_phase_10_48_remediation_gates.py

git diff --check
```

Expected: PASS.

- [ ] **Step 15: Commit Task 5**

Run:

```bash
git add \
  scripts/audit/verify_phase_10_48_ruff_baseline.py \
  tests/domains/test_phase_10_48_remediation_gates.py

git diff --cached --check
git diff --cached --name-only

git commit -m \
  "test(domains): add phase 10.48 baseline-aware quality gates"
```

---

### Task 6: Correct Decimal Reference Documentation

**Findings:** `MINOR-02`

**Files:**
- Modify: `docs/reference/domain-quality-metrics.md`

**Interfaces:**
- Consumes: audited existing rational/Decimal implementation
- Produces: documentation that accurately distinguishes terminating and non-terminating weighted ratios
- Preserves: all public contract semantics

- [ ] **Step 1: Replace the inaccurate non-terminating-ratio statement**

Find the paragraph that currently says, in substance:

```text
Finite Decimal operands therefore never lose precision regardless of ambient
context precision. A non-terminating ratio (impossible for the bounded
operations over finite decimals in practice) ...
```

Replace it with wording equivalent to:

```markdown
Weighted operands are converted exactly to rational values before division.
When the normalized rational has a terminating decimal expansion, reconstruction
is exact. Finite Decimal inputs can still produce a non-terminating normalized
ratio (for example, a value equivalent to `2/3`); in that case the contract uses
deterministic bounded `ROUND_HALF_EVEN` reconstruction derived from operand
magnitudes. The result is therefore independent of the process-global Decimal
context even when exact finite Decimal representation is impossible.
```

Do not claim a non-terminating rational can be represented exactly as a finite Decimal.

- [ ] **Step 2: Ensure the weight-normalization section remains accurate**

The reference must continue to state:

```text
weights need not sum to 1
normalized weighted arithmetic preserves relative priority
first-party catalogs currently sum to exactly Decimal("1")
```

Do not alter first-party policy.

- [ ] **Step 3: Verify documentation and contract tests**

Run:

```bash
grep -n -i \
  'non-terminating\|weights need not sum' \
  docs/reference/domain-quality-metrics.md

.venv/bin/python -m pytest \
  tests/domains/test_domain_quality_contracts.py \
  -q

git diff --check
```

Expected: PASS.

- [ ] **Step 4: Commit Task 6**

Run:

```bash
git add docs/reference/domain-quality-metrics.md

git diff --cached --check
git diff --cached --name-only

git commit -m \
  "docs(domains): correct quality decimal reference semantics"
```

---

### Task 7: Record Remediation State Without Claiming Independent Verification

**Findings:** all four findings

**Files:**
- Modify: `docs/reference/domain-intelligence-requirements-matrix.md`
- Modify: `docs/roadmap/phase-10-domain-intelligence.md`
- Modify: `ROADMAP.md`

**Interfaces:**
- Consumes: Audit V1 FAIL
- Consumes: remediation commits from Tasks 1–6
- Produces: truthful pre-Re-audit-V2 state
- Preserves: all historical closure/audit records

- [ ] **Step 1: Update the DP-048 requirements-matrix evidence**

The live DP-048 row must add evidence equivalent to:

```text
INDEPENDENT_AUDIT_V1=FAIL
BLOCKERS=0
MAJORS=2
MINORS=2
MAJOR_01=REMEDIATED_REPORTED
MAJOR_02=REMEDIATED_REPORTED
MINOR_01=REMEDIATED_REPORTED
MINOR_02=REMEDIATED_REPORTED
PHASE10_48=IMPLEMENTED_PENDING_INDEPENDENT_REAUDIT_V2
DP-048=IMPLEMENTED_PENDING_INDEPENDENT_VERIFICATION
AT-DP-048=PASS_REPORTED
CLOSURE_ELIGIBLE=NO
```

Reference:

```text
docs/audits/phase-10.48-independent-audit-v1.md
docs/superpowers/specs/2026-09-10-phase-10.48-remediation-v1-design.md
docs/superpowers/plans/2026-09-10-phase-10.48-remediation-v1-implementation-plan.md
scripts/audit/verify_phase_10_48_ruff_baseline.py
```

Do not write `VERIFIED_REMEDIATED` before Independent Re-audit V2.

- [ ] **Step 2: Update the detailed roadmap current Phase 10.48 state**

Keep the historical Phase 10.47 closure block containing `PHASE10_48=NOT_STARTED` untouched.

Update or append the current Phase 10.48 status block to:

```text
PHASE10_48=IMPLEMENTED_PENDING_INDEPENDENT_REAUDIT_V2
INDEPENDENT_AUDIT_V1=FAIL
BLOCKERS=0
MAJORS=2
MINORS=2
MAJOR_01=REMEDIATED_REPORTED
MAJOR_02=REMEDIATED_REPORTED
MINOR_01=REMEDIATED_REPORTED
MINOR_02=REMEDIATED_REPORTED
DP-048=IMPLEMENTED_PENDING_INDEPENDENT_VERIFICATION
AT-DP-048=PASS_REPORTED
CLOSURE_ELIGIBLE=NO
```

- [ ] **Step 3: Update `ROADMAP.md` current status**

Update the top/current Phase 10.48 status and its detailed entry to “implemented, remediation V1 complete, pending Independent Re-audit V2” with the same non-closure markers.

Do not mark Phase 10.48 closed.

- [ ] **Step 4: Verify no forbidden closure language was introduced**

Run:

```bash
python3 - <<'PY'
from pathlib import Path

paths = (
    Path("docs/reference/domain-intelligence-requirements-matrix.md"),
    Path("docs/roadmap/phase-10-domain-intelligence.md"),
    Path("ROADMAP.md"),
)

for path in paths:
    text = path.read_text(encoding="utf-8")
    assert "PHASE10_48=IMPLEMENTED_PENDING_INDEPENDENT_REAUDIT_V2" in text
    assert "CLOSURE_ELIGIBLE=NO" in text

print("PHASE10_48_REMEDIATION_DOC_STATE=PASS")
PY
```

Also verify the historical 10.47 block still exists:

```bash
grep -n -F 'PHASE10_48=NOT_STARTED' \
  docs/roadmap/phase-10-domain-intelligence.md
```

Expected: the historical marker remains present.

- [ ] **Step 5: Run diff check and commit**

Run:

```bash
git diff --check

git add \
  docs/reference/domain-intelligence-requirements-matrix.md \
  docs/roadmap/phase-10-domain-intelligence.md \
  ROADMAP.md

git diff --cached --check
git diff --cached --name-only

git commit -m \
  "docs(domains): record phase 10.48 remediation v1"
```

---

### Task 8: Run Focused Phase 10.48 Remediation Gates

**Findings:** all four

**Files:**
- No new production files expected
- Any failure must be fixed in the narrow owning file before continuing

**Interfaces:**
- Consumes: Tasks 1–7
- Produces: fresh focused verification evidence

- [ ] **Step 1: Verify the four findings with focused tests**

Run:

```bash
.venv/bin/python -m pytest \
  tests/domains/test_domain_quality_contracts.py \
  tests/domains/test_domain_quality_pack_integration.py \
  tests/domains/test_domain_quality_first_party.py \
  tests/domains/test_domain_quality_architecture.py \
  tests/domains/test_domain_quality_dp048_acceptance.py \
  tests/domains/test_domain_prompt_clause_coverage.py \
  tests/domains/test_phase_10_48_remediation_gates.py \
  -q
```

Expected: PASS.

- [ ] **Step 2: Re-run the baseline-aware Ruff/format gate**

Run:

```bash
.venv/bin/python \
  scripts/audit/verify_phase_10_48_ruff_baseline.py \
  | tee "$HOME/Library/Mobile Documents/com~apple~CloudDocs/Downloads/phase-10.48-remediation-v1-ruff-baseline-final.txt"
```

Required:

```text
CHANGED_PYTHON_RUFF=PASS
CHANGED_PYTHON_FORMAT=PASS
NO_NEW_RUFF_REGRESSIONS=PASS
NO_NEW_FORMAT_REGRESSIONS=PASS
BASELINE_AWARE_GATE=PASS
```

Record the measured global baseline/current values exactly.

- [ ] **Step 3: Run explicit Ruff/format on all Python files changed since baseline**

Generate the list:

```bash
BASELINE="35bf9d2b33c9e3c31ecd789c5d1237590eb31816"

CHANGED_PY="$(
  {
    git diff --name-only --diff-filter=ACMRT "$BASELINE" -- '*.py'
    git ls-files --others --exclude-standard -- '*.py'
  } | sort -u
)"

printf '%s\n' "$CHANGED_PY"
test -n "$CHANGED_PY"

printf '%s\n' "$CHANGED_PY" | \
  xargs .venv/bin/python -m ruff check

printf '%s\n' "$CHANGED_PY" | \
  xargs .venv/bin/python -m ruff format --check
```

Expected: PASS.

- [ ] **Step 4: Verify the global commands truthfully**

Run them, but do not expect zero if legacy debt remains:

```bash
set +e

.venv/bin/python -m ruff check .
GLOBAL_RUFF_EXIT=$?

.venv/bin/python -m ruff format --check .
GLOBAL_FORMAT_EXIT=$?

set -e

echo "GLOBAL_RUFF_EXIT=$GLOBAL_RUFF_EXIT"
echo "GLOBAL_FORMAT_EXIT=$GLOBAL_FORMAT_EXIT"
```

Interpretation:

- `0` means the corresponding global gate genuinely passes;
- `1` with baseline-equivalent/reduced debt is historical debt and must be reported as such;
- any execution error other than the expected Ruff “violations found / would reformat” result is a gate failure.

Do not print a false global PASS marker.

- [ ] **Step 5: Verify Git diff hygiene**

Run:

```bash
git diff --check
git status --short
```

Expected: clean if all previous tasks were committed.

---

### Task 9: Run Phase 10.46 and Phase 10.47 Regressions

**Files:**
- No changes expected

**Interfaces:**
- Produces: proof that remediation did not disturb previously closed phases

- [ ] **Step 1: Run Phase 10.46 model-policy regressions**

Run the same focused 10.46 contract/serialization/architecture/acceptance test set used by the Phase 10.48 implementation handoff.

At minimum include files matching:

```text
tests/domains/test_domain_model_policy*
tests/domains/*dp046*
```

Resolve exact existing filenames first with:

```bash
find tests/domains -maxdepth 1 -type f \
  \( -name '*model_policy*' -o -name '*dp046*' \) \
  -print | sort
```

Then run all returned Phase 10.46 files with `.venv/bin/python -m pytest ... -q`.

Expected: PASS.

- [ ] **Step 2: Run Phase 10.47 benchmark regressions**

Run:

```bash
.venv/bin/python -m pytest \
  tests/domains/test_domain_benchmark_contracts.py \
  tests/domains/test_domain_benchmark_pack_integration.py \
  tests/domains/test_domain_benchmark_first_party.py \
  tests/domains/test_domain_benchmark_architecture.py \
  tests/domains/test_domain_benchmark_dp047_acceptance.py \
  -q
```

Expected: PASS.

- [ ] **Step 3: Verify benchmark fields remain unweighted**

Run:

```bash
.venv/bin/python - <<'PY'
from dataclasses import fields

from cmm.domains.benchmark_contracts import DomainBenchmarkCase

names = {field.name for field in fields(DomainBenchmarkCase)}

for forbidden in (
    "weight",
    "minimum_score",
    "blocking",
    "aggregate_score",
    "quality_metrics",
):
    assert forbidden not in names

print("PHASE10_47_BENCHMARK_SEMANTICS=PRESERVED")
PY
```

Expected: PASS.

---

### Task 10: Run Domain and Global Regression Gates

**Files:**
- No changes expected

**Interfaces:**
- Produces: fresh subsystem/global evidence before exact-HEAD audit creation

- [ ] **Step 1: Run relevant Domain integration regressions**

Run the canonical groups covering:

```text
DomainDefinition serialization/parsing
Domain Pack parsing/loading
Domain public API
DomainRegistry
observability metrics
```

Use the exact existing files discovered in the repository; do not silently skip an existing matching test.

A reproducible discovery command is:

```bash
find tests/domains -maxdepth 1 -type f \
  \( \
    -name '*contract*' -o \
    -name '*pack*' -o \
    -name '*public_api*' -o \
    -name '*registry*' -o \
    -name '*observability*' \
  \) \
  -print | sort
```

Run the relevant canonical test set with `.venv/bin/python -m pytest ... -q`.

Expected: PASS.

- [ ] **Step 2: Run the complete Domain suite**

Run:

```bash
.venv/bin/python -m pytest tests/domains -q
```

Expected: PASS.

- [ ] **Step 3: Run the complete global suite**

Run:

```bash
.venv/bin/python -m pytest -q
```

Expected: PASS.

Do not rely on the previous `16378 passed` handoff; this must be a fresh post-remediation run.

- [ ] **Step 4: Compile production packages**

Run:

```bash
.venv/bin/python -m compileall -q \
  cmm \
  kernel
```

Expected: exit 0.

- [ ] **Step 5: Run final diff and worktree checks**

Run:

```bash
git diff --check
test -z "$(git status --porcelain)"

git status --short --branch
git stash list
```

Expected:

```text
WORKTREE=CLEAN
QUARANTINE_STASH=PRESERVED
```

If any test or gate fails, stop and remediate that regression before continuing.

---

### Task 11: Verify Exact Remediation Scope and Historical Artifact Immutability

**Files:**
- No changes expected

**Interfaces:**
- Produces: reviewable scope proof before bundle creation

- [ ] **Step 1: List all changes since the remediation starting point**

The remediation design was committed at:

```text
b8401642af5e40a52ab0c9d1ae91e93430267e5f
```

Run:

```bash
git diff \
  --name-status \
  b8401642af5e40a52ab0c9d1ae91e93430267e5f..HEAD
```

Expected changes are limited to the remediation files defined by this plan.

- [ ] **Step 2: Prove Audit V1 is unchanged**

Run:

```bash
git diff \
  b8401642af5e40a52ab0c9d1ae91e93430267e5f..HEAD \
  -- docs/audits/phase-10.48-independent-audit-v1.md
```

Expected: no output.

- [ ] **Step 3: Prove original Phase 10.48 spec/plan are unchanged**

Run:

```bash
git diff \
  b8401642af5e40a52ab0c9d1ae91e93430267e5f..HEAD \
  -- \
  docs/superpowers/specs/2026-09-10-phase-10.48-domain-quality-metrics-design.md \
  docs/superpowers/plans/2026-09-10-phase-10.48-domain-quality-metrics-implementation-plan.md \
  docs/superpowers/specs/2026-09-10-phase-10.48-remediation-v1-design.md
```

Expected: no output.

- [ ] **Step 4: Verify first-party quality policy is unchanged**

Run the complete first-party test:

```bash
.venv/bin/python -m pytest \
  tests/domains/test_domain_quality_first_party.py \
  -q
```

Expected: PASS.

Also verify no first-party quality catalog changed during remediation:

```bash
git diff \
  --name-only \
  b8401642af5e40a52ab0c9d1ae91e93430267e5f..HEAD \
  -- 'cmm/domains/*/quality_metrics.py'
```

Expected: no output.

---

### Task 12: Produce the New Exact-HEAD Audit Bundle

**Files:**
- No repository changes expected

**Interfaces:**
- Consumes: fully committed clean remediation HEAD
- Produces: new exact-HEAD TAR.GZ and SHA-256
- Preserves: V1 bundle and V1 report

- [ ] **Step 1: Capture final remediation HEAD and invariants**

Run:

```bash
set -euo pipefail

REPO="$HOME/CMM OS"
DL="$HOME/Library/Mobile Documents/com~apple~CloudDocs/Downloads"

cd "$REPO"

REMEDIATION_HEAD="$(git rev-parse HEAD)"
SHORT_HEAD="$(git rev-parse --short=12 HEAD)"

test -z "$(git status --porcelain)"

STASH_SNAPSHOT="$(git stash list)"
printf '%s\n' "$STASH_SNAPSHOT" | grep -F \
  "quarantine: post-audit phase 10.32 uncommitted changes"

echo "REMEDIATION_HEAD=$REMEDIATION_HEAD"
echo "WORKTREE=CLEAN"
echo "QUARANTINE_STASH=PRESERVED"
```

- [ ] **Step 2: Create a new bundle; never overwrite V1**

Run:

```bash
BUNDLE="$DL/phase-10.48-reaudit-v2-${SHORT_HEAD}.tar.gz"

test ! -e "$BUNDLE"

git archive \
  --format=tar.gz \
  --prefix="CMM-OS/" \
  -o "$BUNDLE" \
  HEAD
```

Do not reuse:

```text
phase-10.48-audit-f588c7b79fa1.tar.gz
```

- [ ] **Step 3: Verify gzip integrity, exact commit ID, and SHA-256**

Run:

```bash
gzip -t "$BUNDLE"

ARCHIVE_COMMIT_ID="$(
  gzip -dc "$BUNDLE" | git get-tar-commit-id
)"

test "$ARCHIVE_COMMIT_ID" = "$REMEDIATION_HEAD"

BUNDLE_SHA="$(
  shasum -a 256 "$BUNDLE" | awk '{print $1}'
)"

echo "AUDITED_IMPLEMENTATION_HEAD=$REMEDIATION_HEAD"
echo "ARCHIVE_COMMIT_ID=$ARCHIVE_COMMIT_ID"
echo "AUDIT_BUNDLE=$BUNDLE"
echo "AUDIT_BUNDLE_SHA256=$BUNDLE_SHA"
echo "EXACT_HEAD_BUNDLE=PASS"
```

- [ ] **Step 4: Re-run clean-state verification after bundle creation**

Run:

```bash
test -z "$(git status --porcelain)"
test "$(git stash list)" = "$STASH_SNAPSHOT"

echo "WORKTREE=CLEAN"
echo "QUARANTINE_STASH=PRESERVED"
echo "PUSH=NO"
echo "MERGE=NO"
```

Expected: PASS.

---

## Final Implementation Handoff

The implementation agent's final response must report only fresh, evidenced values.

Required status vocabulary:

```text
PHASE10_48=IMPLEMENTED_PENDING_INDEPENDENT_REAUDIT_V2

INDEPENDENT_AUDIT_V1=FAIL
BLOCKERS=0
MAJORS=2
MINORS=2

MAJOR_01=REMEDIATED_REPORTED
MAJOR_02=REMEDIATED_REPORTED
MINOR_01=REMEDIATED_REPORTED
MINOR_02=REMEDIATED_REPORTED

DP-048=IMPLEMENTED_PENDING_INDEPENDENT_VERIFICATION
AT-DP-048=PASS_REPORTED
CLOSURE_ELIGIBLE=NO
```

It must include:

```text
REMEDIATION_START_HEAD=<exact prompt-supplied implementation start>
REMEDIATION_HEAD=<exact committed HEAD>

FOCUSED_TESTS=<fresh count>
PHASE10_46_REGRESSIONS=<fresh result/count>
PHASE10_47_REGRESSIONS=<fresh result/count>
DOMAIN_SUITE=<fresh count>
GLOBAL_SUITE=<fresh count>
COMPILEALL=PASS
GIT_DIFF_CHECK=PASS

RUFF_VERSION=<measured>
GLOBAL_RUFF_BASELINE=<measured>
GLOBAL_RUFF_CURRENT=<measured>
GLOBAL_FORMAT_BASELINE=<measured>
GLOBAL_FORMAT_CURRENT=<measured>
CHANGED_PYTHON_RUFF=PASS
CHANGED_PYTHON_FORMAT=PASS
NO_NEW_RUFF_REGRESSIONS=PASS
NO_NEW_FORMAT_REGRESSIONS=PASS
BASELINE_AWARE_GATE=PASS

R10_C48_CLAUSE_COUNT=1
CLAUSE_ROWS=<derived>
COVERED_CLAUSES=<derived>
UNCLASSIFIED_CLAUSES=0

HUMAN_REVIEW_NOTES_FAIL_CLOSED=PASS
NON_UNIT_WEIGHT_NORMALIZATION=PASS
NON_TERMINATING_DECIMAL_CONTEXT_INDEPENDENCE=PASS

FIRST_PARTY_QUALITY_POLICY=PRESERVED
PHASE10_47_BENCHMARK_SEMANTICS=PRESERVED
OBSERVABILITY_SEPARATION=PRESERVED
PARALLEL_QUALITY_INFRASTRUCTURE=ABSENT
MODEL_EXECUTION=ABSENT
PROVIDER_EXECUTION=ABSENT
EVALUATOR_EXECUTION=ABSENT

WORKTREE=CLEAN
QUARANTINE_STASH=PRESERVED
PUSH=NO
MERGE=NO

AUDIT_BUNDLE=<new path>
AUDIT_BUNDLE_SHA256=<new sha256>
EXACT_HEAD_BUNDLE=PASS

NEXT=INDEPENDENT_CHATGPT_REAUDIT_V2
```

The angle-bracket fields above describe values that must be populated from the commands executed in the implementation session; the agent must not guess or reuse historical values.

The implementation agent must disclose whole-repository Ruff/format debt truthfully. If the global current counts are non-zero, the handoff must not claim global Ruff or global format PASS.

---

## Acceptance Criteria for Remediation Implementation

The remediation implementation is ready for Independent Re-audit V2 only when:

1. malformed `notes` strings are rejected;
2. malformed `notes` mappings are rejected;
3. malformed `notes` scalars are rejected;
4. malformed nested/non-string note items are rejected;
5. all malformed serialized note shapes surface as `DomainSerializationError`;
6. valid list/tuple human-review notes still round-trip;
7. connected AT-DP-048 covers malformed nested human-review evidence;
8. the non-unit-weight test genuinely uses weights whose total is not one;
9. non-unit normalized aggregation is context-independent;
10. non-terminating normalized aggregation is deterministic across `prec=10/28/50`;
11. Decimal reference documentation matches the actual rational/rounding implementation;
12. `R10-C48` exists exactly once in the coverage ledger;
13. the clause ledger has no duplicate IDs;
14. coverage totals equal actual clause rows;
15. unclassified clauses remain zero;
16. baseline/current global Ruff debt is measured with the same Ruff version;
17. baseline/current global format debt is measured with the same Ruff version;
18. every Python file changed since pre-10.48 baseline is Ruff-clean;
19. every Python file changed since pre-10.48 baseline is Ruff-format-clean;
20. global Ruff debt has not increased from baseline;
21. global format debt has not increased from baseline;
22. no false global PASS claim is emitted;
23. Phase 10.46 regressions pass;
24. Phase 10.47 regressions pass;
25. Phase 10.47 benchmark contracts remain unweighted;
26. first-party quality catalogs remain unchanged;
27. observability separation remains unchanged;
28. no parallel quality infrastructure appears;
29. AT-DP-048 passes;
30. the complete Domain suite passes;
31. the complete global suite passes;
32. `compileall` passes;
33. `git diff --check` passes;
34. Audit V1 remains unchanged;
35. original 10.48 design/plan remain unchanged;
36. remediation design remains unchanged;
37. documentation says pending Independent Re-audit V2, not closed;
38. all remediation changes are committed;
39. worktree is clean;
40. quarantine stash is preserved;
41. new exact-HEAD bundle exists;
42. bundle commit ID equals remediation HEAD;
43. new bundle SHA-256 is reported;
44. V1 bundle is not overwritten;
45. no push or merge occurred.

---

## Stop Conditions

Stop implementation immediately and report instead of broadening scope if:

- the remediation requires changing first-party quality policy;
- the remediation requires changing `DomainDefinition.quality_metrics`;
- the remediation requires changing benchmark semantics;
- the remediation requires repurposing observability;
- the remediation appears to require model/provider/evaluator execution;
- the baseline-aware gate shows Phase 10.48 introduced new Ruff/format debt that cannot be fixed inside the changed files;
- a global/domain regression failure is unrelated and repairing it would require broad unrelated changes;
- the quarantine stash changes;
- the implementation starting branch/HEAD differs from the implementation prompt;
- the worktree is unexpectedly dirty before implementation.

In such a case, do not improvise a wider architecture or maintenance phase.

---

## Final Plan Statement

This remediation is a correction pass, not a second implementation of Phase 10.48.

The intended delta is:

```text
malformed human-review serialization
    → fail closed

false Decimal test/document wording
    → accurate executable evidence

missing/stale R10-C48 coverage
    → self-consistent traceability

false whole-repo lint/format PASS expectation
    → truthful reproducible legacy-baseline no-regression gate
```

Everything else already verified by Independent Audit V1 must remain stable.
