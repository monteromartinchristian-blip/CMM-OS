# Phase 10.45 — Audit V1 Remediation Plan (All MAJORs)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remediate all four MAJOR findings of Independent Audit V1 (`MAJOR-01` canonical authority binding, `MAJOR-02` conversational result refs, `MAJOR-03` selector add/withdraw cannot complete, `MAJOR-04` Review Center unresolved conflicts) with TDD, then pass every gate of the remediation prompt and produce the final exact-HEAD re-audit bundle.

**Architecture:** Extend the existing Phase 10.45 interface projection/intent seam with canonical authority binding (session and memory/knowledge), canonical conversational result refs, one minimal canonical Domain Selection Transition Coordinator (per the design amendment), reference-only Review Center conflict items, and a strengthened connected `AT-DP-045`. No parallel stores, resolvers, composers, session systems, permission engines, or interface-owned authority.

**Tech Stack:** Python 3, frozen/slotted dataclasses and existing CMM OS contract conventions, canonical `cmm.domains` infrastructure, pytest, Ruff, Python `compileall`, Git.

**Spec:** `docs/superpowers/specs/2026-09-08-phase-10.45-integration-with-interfaces-design.md` plus `docs/superpowers/specs/2026-09-09-phase-10.45-major-03-selection-transition-amendment.md`.

**Implementation prompt:** `~/Library/Mobile Documents/com~apple~CloudDocs/Downloads/phase-10.45-full-remediation-all-majors-prompt.md` (authoritative; this plan is its §5.2 artifact). No other implementation prompt is created.

## Global Constraints

- Start of remediation implementation: HEAD `d228707` (design amendment commit), branch `feature/phase-10-domain-intelligence`, clean worktree, quarantine stash `stash@{0}` preserved.
- TDD per block: `TEST RED → MINIMAL IMPLEMENTATION → GREEN → CONTROLLED REFACTOR → RELEVANT REGRESSIONS → COMMIT → WORKTREE CLEAN → NEXT BLOCK`. Do not implement several majors before proving each one.
- Do not weaken existing assertions. Do not delete failing adversarial tests.
- Do not modify `docs/audits/phase-10.45-independent-audit-v1.md` or `docs/audits/phase-10.45-major-03-architectural-block-evidence.md`.
- Do not overwrite bundles `phase-10.45-audit-1a1c29795d572e97e2f34765da979c558a6b179e.tar.gz` and `phase-10.45-reaudit-v2-42f94c9c4274e18f3fc33e747bb3ae573a69ed4b.tar.gz`.
- Never run without explicit user authorization: `git push`, `git merge`, `git rebase`, `git stash`, `git stash pop`, `git stash apply`, `git stash drop`, `git reset`, `git clean`, `git worktree`.
- After every commit: `test -z "$(git status --porcelain)` must pass.
- Never write `DP_045=VERIFIED_EXISTING`, `PHASE10_45=CLOSED`, `CLOSURE_ELIGIBLE=YES`, `AT_DP_045=INDEPENDENTLY_VERIFIED`. Pre-reaudit tokens are fixed in Block 8.
- Do not close Phase 10.45. Do not start Phase 10.46. Do not self-audit closure.
- Canonical errors propagate unchanged. No `**kwargs`. No untyped authority dictionaries. No `Any`/`object` authority payloads unless an existing repository protocol forces it.
- Do not create another API facade, implementation prompt, or acceptance file.

---

## Block 0 — Precheck

Read-only re-anchoring before remediation code.

- [ ] **Step 0.1:** Verify branch `feature/phase-10-domain-intelligence`, clean worktree, quarantine stash `stash@{0}` present, HEAD is the plan commit that follows `d228707`.
- [ ] **Step 0.2:** Verify all six dedicated files exist and run them once to record the pre-remediation baseline:
  - `tests/domains/test_domain_interface_integration_contracts.py`
  - `tests/domains/test_domain_interface_integration.py`
  - `tests/domains/test_domain_interface_integration_api.py`
  - `tests/domains/test_domain_interface_integration_architecture.py`
  - `tests/domains/test_domain_interface_dp045_acceptance.py`
- [ ] **Step 0.3:** Re-confirm the exact canonical symbols used by the remediation (resolver policy semantics for `required_domains`/`denied_domains`, `save_domain_session(expected_previous_revision=...)`, `build_domain_selection_transition`, `DomainSessionContext` fields, `DomainMemoryKnowledgeProjectionRequest.digest`).

---

## Block 1 — MAJOR-01: canonical authority binding

**Files:** `cmm/domains/interface_integration.py`, `cmm/domains/interface_integration_contracts.py`, `cmm/domains/api.py`, `tests/domains/test_domain_interface_integration.py`, `tests/domains/test_domain_interface_dp045_acceptance.py` (mechanical signature updates only).

- [ ] **Step 1.1 (RED):** Add at minimum these tests to `test_domain_interface_integration.py`:
  - `test_projection_rejects_session_composition_mismatch`
  - `test_projection_rejects_session_resolution_mismatch`
  - `test_projection_rejects_session_primary_domain_mismatch`
  - `test_projection_rejects_session_supporting_domain_mismatch`
  - `test_projection_rejects_foreign_memory_knowledge_projection`
  - `test_projection_accepts_bound_memory_knowledge_projection`
  Run them; they must fail (RED). Do not weaken existing binding assertions.
- [ ] **Step 1.2 (GREEN):** Minimal implementation in `DefaultDomainInterfaceIntegrator.project` (and its protocol): when a `DomainSessionContext` is supplied validate `session.composition_id == composition.id`, `session.last_resolution_id == resolution.id`, `session.primary_domain == str(resolution.primary_domain)`, `session.supporting_domains == canonical resolution/composition supporting domains`; apply exact canonical semantics for optional fields (do not reject legitimate absent optional refs). When a memory/knowledge projection is supplied, also accept the canonical `DomainMemoryKnowledgeProjectionRequest` and validate `projection.request_id == request.request_id`, `projection.request_digest == request.digest`, `request.resolution_reference_id == resolution.id`, `request.composition_reference_id == composition.id`, `request.primary_domain == resolution/composition primary`, `request.supporting_domains == canonical supporting domains`. Extend `DefaultDomainAPI.project_interface` (protocol + impl) minimally with exact typed forwarding. Keep Phase 10.44 authority rules; do not derive authority from metadata; do not invent another memory binding contract.
- [ ] **Step 1.3:** Relevant regressions (Block 0 baseline files). Fix any regression before committing.
- [ ] **Step 1.4:** Commit alone and verify clean worktree:
  - `fix(domains): bind phase 10.45 canonical authority coherently`

---

## Block 2 — MAJOR-02: conversational canonical result refs

**Files:** `cmm/domains/interface_integration.py`, `tests/domains/test_domain_interface_integration.py`.

- [ ] **Step 2.1 (RED):** Add `test_conversational_view_surfaces_canonical_cross_domain_result_ref` — a canonical `CrossDomainResult` passing authority binding must surface its canonical result ID in `projection.conversational.result_refs`. Run; must fail.
- [ ] **Step 2.2 (GREEN):** Minimal implementation in the conversational projector: include the canonical result ID in `result_refs`. No duplicated result payload, no new result object, no fabricated IDs; multiple references use deterministic existing ordering.
- [ ] **Step 2.3:** Relevant regressions.
- [ ] **Step 2.4:** Commit alone; clean worktree:
  - `fix(domains): surface canonical conversational result references`

---

## Block 3 — MAJOR-03: selection transition coordinator contracts

**Files (new):** `cmm/domains/selection_transition_contracts.py`, `tests/domains/test_domain_selection_transition.py`.

- [ ] **Step 3.1 (RED):** Contracts tests first: immutable typed `DomainSelectionTransitionCommandKind` (`ADD_SUPPORTING | WITHDRAW_SUPPORTING`), `DomainSelectionTransitionRequest`, `DomainSelectionTransitionResult`, and `DomainSelectionTransitionCoordinator` protocol; validation of typed fields; JSON round-trip per repository conventions; reuse (not duplication) of existing `DomainSelectionTransition`.
- [ ] **Step 3.2 (GREEN):** Create `selection_transition_contracts.py` with only the minimum canonical contracts required, following `cmm/domains` contract conventions (frozen/slotted dataclasses, strict validation, `to_dict`/`from_dict`, module docstring, `__all__`). No dictionaries as authority escape hatches; no `Any`/`object` canonical authority payloads.
- [ ] **Step 3.3:** Relevant regressions (contracts pattern tests).
- [ ] **Step 3.4:** Commit alone; clean worktree:
  - `feat(domains): add canonical selection transition contracts`

---

## Block 4 — MAJOR-03: coordinator implementation

**Files (new):** `cmm/domains/selection_transition.py`; **tests:** `tests/domains/test_domain_selection_transition.py`.

- [ ] **Step 4.1 (RED):** Add the nine required connected tests plus adversarial binding/conflict tests:
  - `test_add_supporting_allow_applies_canonical_transition`
  - `test_add_supporting_permission_deny_blocks`
  - `test_add_supporting_approval_required_stays_pending`
  - `test_withdraw_supporting_applies_canonical_transition`
  - `test_transition_persists_exactly_one_new_session_revision`
  - `test_transition_does_not_mutate_registry`
  - `test_transition_does_not_mutate_original_policy`
  - `test_transition_does_not_persist_on_resolution_failure`
  - `test_transition_does_not_persist_on_composition_failure`
  - plus: target already supporting / target is primary / target disabled / degraded-when-forbidden / unauthorized / optimistic revision conflict / authority-chain binding mismatch / membership delta not actually produced / primary would change.
  Connected fixtures use real registry + real resolver + real composer + `SharedSessionDomainAdapter` over the shared `SessionStore`, mirroring `test_domain_interface_dp045_acceptance.py`.
- [ ] **Step 4.2 (GREEN):** `DefaultDomainSelectionTransitionCoordinator`, dependency injected (resolver, composer, domain_registry, permission_resolver, session_adapter, clock). Behavior per the design amendment: bind current session/resolution/composition authority; validate target lifecycle/eligibility/preconditions; permission via `DomainPermissionResolver` (`ALLOW` proceeds; `DENY` blocked; `APPROVAL_REQUIRED` pending; neither persists); request-scoped derived `DomainResolutionPolicy` clone (`required_domains` for add, `denied_domains` for withdraw) with the original policy never mutated; canonical re-resolution must stay `RESOLVED` with the delta actually applied and primary preserved; canonical recomposition via `DefaultDomainComposer.compose` must be `COMPOSED` or `PARTIAL`; `build_domain_selection_transition(previous, current)`; exactly one new immutable `DomainSessionContext` revision (revision+1) persisted via `session_adapter.save_domain_session(new_context, expected_previous_revision=current.revision)`; typed result. No persistence before all pure authority computation succeeds; no registry mutation; no policy mutation; no direct session patch; no store ownership; no imports from interface integration modules.
- [ ] **Step 4.3:** Relevant regressions (Phase 10.31 selection, 10.34 sessions, 10.8 composition, resolver tests).
- [ ] **Step 4.4:** Commit alone; clean worktree:
  - `feat(domains): implement canonical selection transition coordinator`

---

## Block 5 — MAJOR-03: Phase 10.45 selector delegation + architecture guards

**Files:** `cmm/domains/interface_integration.py`, `cmm/domains/interface_integration_contracts.py`, `cmm/domains/api.py`, `tests/domains/test_domain_interface_integration.py`, `tests/domains/test_domain_interface_integration_architecture.py`, `tests/domains/test_domain_interface_dp045_acceptance.py` (mechanical updates only).

- [ ] **Step 5.1 (RED):** Delegation tests: `ADD_SUPPORTING` with permission `ALLOW` completes through the coordinator (no longer `UNAVAILABLE`); `WITHDRAW_SUPPORTING` completes; `DENY`/`APPROVAL_REQUIRED` verdicts preserved; interface layer performs no transition algorithm itself.
- [ ] **Step 5.2 (GREEN):** Extend `DefaultDomainInterfaceIntegrator` (protocol + impl) and its constructor to hold the coordinator dependency; `ADD_SUPPORTING`/`WITHDRAW_SUPPORTING` submit a typed `DomainSelectionTransitionRequest` and project the typed canonical result. `AUTO_RESOLVE` and `SELECT_PRIMARY` continue through existing canonical resolver authority unchanged; `EXPLAIN_SELECTION` unchanged; `REQUEST_POLICY_CHANGE` remains `UNAVAILABLE` (no canonical write authority exists). Extend `DefaultDomainAPI` minimally with exact typed forwarding and wiring when a public seam is required; no second facade.
- [ ] **Step 5.3 (GREEN):** Extend `test_domain_interface_integration_architecture.py` to forbid at minimum: `SelectionTransitionStore`, `SelectionTransitionRepository`, `InterfaceSelectionStore`, `InterfaceSessionStore`, `InterfaceCompositionEngine`, `InterfaceResolver`, `InterfacePermissionEngine`, `InterfaceReviewStore`, `InterfaceMemoryStore`, `InterfaceRuntime`, `InterfaceOrchestrator`; verify `selection_transition.py` does not own persistence (may call the existing session adapter); verify dependency direction `interface_integration → selection_transition → resolver/composer/permission/session` and never the reverse; earlier canonical Phase 10 modules must not import Phase 10.45 interface modules.
- [ ] **Step 5.4:** Relevant regressions (permission 10.15, resolution, composition, sessions, API).
- [ ] **Step 5.5:** Commit alone (impl + tightly coupled delegation tests + guard tests); clean worktree:
  - `fix(domains): delegate phase 10.45 selector membership transitions`

---

## Block 6 — MAJOR-04: Review Center unresolved conflicts

**Files:** `cmm/domains/interface_integration.py`, `tests/domains/test_domain_interface_integration.py`.

- [ ] **Step 6.1 (RED):** `test_review_center_surfaces_unresolved_review_required_cross_domain_conflict` — canonical `CrossDomainResult` with a contradiction `requires_review=True`, `resolved=False` (status `REQUIRES_REVIEW` in the acceptance context) must expose a reference-only `DomainReviewItemView` preserving canonical contradiction id, review-required status, unresolved-conflict category, and safe reference/reason semantics. Run; must fail.
- [ ] **Step 6.2 (GREEN):** Minimal implementation in the Review Center projector consuming canonical cross-domain conflict state. No `ApprovalRequest` created for conflicts, no review store, no contradiction mutation, reference-only.
- [ ] **Step 6.3:** Relevant regressions.
- [ ] **Step 6.4:** Commit alone; clean worktree:
  - `fix(domains): surface unresolved conflicts for interface review`

---

## Block 7 — AT-DP-045 correction

**Files:** `tests/domains/test_domain_interface_dp045_acceptance.py`.

- [ ] **Step 7.1:** Strengthen the existing connected acceptance (no separate superficial acceptance). The corrected chain proves: real registry → real resolver → real composition → canonical session → presentation → workflow/operation state → permission state → approval state → observability → Phase 10.44 memory/knowledge projection with canonical request binding → canonical selection transition coordinator → `DefaultDomainAPI` → `DefaultDomainInterfaceIntegrator` → five views.
- [ ] **Step 7.2:** Corrected positive assertions: canonical result appears in conversational `result_refs`; `ADD_SUPPORTING` succeeds after permission `ALLOW`; `WITHDRAW_SUPPORTING` succeeds; new session revision is canonical and persisted; review-required unresolved conflict appears in Review Center; memory/knowledge projection is authority-bound.
- [ ] **Step 7.3:** Adversarial assertions retain all original twelve branches and add: session/composition mismatch fails closed; session/resolution mismatch fails closed; foreign memory projection fails closed; selector transition persistence conflict fails closed; selector transition never mutates registry/policy.
- [ ] **Step 7.4:** Only print `AT-DP-045=PASS` after all corrected assertions pass.
- [ ] **Step 7.5:** Commit alone; clean worktree:
  - `test(domains): strengthen AT-DP-045 after audit v1`

---

## Block 8 — Documentation truth

**Files:** `docs/reference/domain-interface-integration.md`, `docs/reference/domain-intelligence-requirements-matrix.md`, `docs/roadmap/phase-10-domain-intelligence.md`, `ROADMAP.md` (plus a reference-doc note for the new selection-transition authority if repository conventions require it).

- [ ] **Step 8.1:** Update the four documents so they state the remediated truth (authority binding, conversational result refs, canonical selection transition coordinator, Review Center unresolved conflicts).
- [ ] **Step 8.2:** Preserve the Audit V1 report and MAJOR-03 architectural-block evidence unchanged. Write exactly the pre-reaudit state:
  - `PHASE10_45=REMEDIATED_PENDING_INDEPENDENT_REAUDIT`
  - `DP_045=IMPLEMENTED_PENDING_INDEPENDENT_VERIFICATION`
  - `AT_DP_045=PASS_REPORTED`
  - `CLOSURE_ELIGIBLE=NO`
- [ ] **Step 8.3:** Commit alone; clean worktree:
  - `docs(domains): document phase 10.45 audit v1 remediation`

---

## Block 9 — Focused regressions

- [ ] **Step 9.1:** Run the Phase 10.45 dedicated files (Block 0 list) plus all new selection-transition tests: PASS.
- [ ] **Step 9.2:** Run focused regressions covering Phase 10.15 permissions, 10.16 presentation, 10.31 selection policies, 10.32 conflict resolution, 10.34 sessions, 10.36 Domain API, 10.37 observability, 10.40 cognitive integration, 10.41 Agent Runtime integration, 10.42 planner/workflow integration, 10.43 validation integration, 10.44 memory/knowledge integration: PASS. Fix any regression before proceeding.

---

## Block 10 — Subsystem and global gates

- [ ] **Step 10.1:** `.venv/bin/python -m pytest -q tests/domains` → PASS.
- [ ] **Step 10.2:** `.venv/bin/python -m pytest -q` (full suite, foreground, record the fresh final pass count) → PASS.
- [ ] **Step 10.3:** Ruff via the repository canonical gate; if repo-wide Ruff has known pre-existing failures, prove the remediation delta is clean and record the pre-existing baseline separately — never claim repo-wide PASS unless actually clean.
- [ ] **Step 10.4:** Repository canonical formatter check → PASS.
- [ ] **Step 10.5:** `.venv/bin/python -m compileall -q cmm` → PASS.
- [ ] **Step 10.6:** `git diff --check` → PASS.
- [ ] **Step 10.7:** Architecture guards → PASS.

---

## Block 11 — Exact-HEAD re-audit bundle and final report

Only after `MAJOR_01=FIXED`, `MAJOR_02=FIXED`, `MAJOR_03=FIXED`, `MAJOR_04=FIXED`, `AT_DP_045=PASS_REPORTED`, all gates complete, all changes committed, worktree clean:

- [ ] **Step 11.1:** Create the bundle exactly per the prompt §18 and hash it:

```bash
REMEDIATED_HEAD="$(git rev-parse HEAD)"
OUT="$HOME/Library/Mobile Documents/com~apple~CloudDocs/Downloads"
BUNDLE="$OUT/phase-10.45-reaudit-v2-${REMEDIATED_HEAD}.tar.gz"
git archive --format=tar.gz --output="$BUNDLE" "$REMEDIATED_HEAD"
BUNDLE_SHA256="$(shasum -a 256 "$BUNDLE" | awk '{print $1}')"
```

- [ ] **Step 11.2:** Verify the archive contains all ten files: the two audit documents, the design amendment, this plan, `cmm/domains/interface_integration.py`, `cmm/domains/interface_integration_contracts.py`, `cmm/domains/selection_transition.py`, `cmm/domains/selection_transition_contracts.py`, `tests/domains/test_domain_interface_dp045_acceptance.py`. Do not modify the archive after hashing.
- [ ] **Step 11.3:** Print the required final report (§19 format, truthful fresh results, full HEADs, bundle path and SHA-256), then **STOP** (§20). Do not self-audit closure, do not perform the docs-only closure commit, do not start Phase 10.46.
