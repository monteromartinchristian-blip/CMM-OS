# Phase 10.30 — Audit V1 Remediation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remediate the 2 blockers and 5 major findings from the independent Phase 10.30 Project Domain audit V1 without reopening the frozen Project architecture or weakening existing acceptance contracts.

**Architecture:** Preserve the canonical 14-module `domain:project` Domain Pack and its frozen `27 / 22 / 18 / 20 / 12` inventory. Repair trust boundaries by reusing the hardened shared/Life Plan authorization, commit-readiness, bootstrap, trace, development, validation, permission, approval, transaction and rollback infrastructure already present in CMM OS. Rebuild AT-DP-030 and the permanent adversarial gate so their labels correspond to real executed behavior.

**Tech Stack:** Python 3.10+, existing CMM OS Phase 7 Validation, Phase 8 Cognitive Layer, Phase 9 Agent Runtime, Phase 10 Domain Intelligence contracts/registries, pytest, Ruff, compileall, Git.

**Spec:** `docs/superpowers/specs/2026-08-26-project-domain-design.md`

**Original implementation plan:** `docs/superpowers/plans/2026-08-26-project-domain-implementation.md`

**Independent audit:** `docs/audits/phase-10.30-project-independent-audit-v1.md`

## Global Constraints

- Remediation baseline anchor: commit `ee67300` (`docs(project): record independent audit v1 failure`) must remain an ancestor of execution `HEAD`.
- Required branch: `feature/phase-10-domain-intelligence`.
- Preserve exactly **14** Python production modules in `cmm/domains/project/`.
- Preserve canonical inventory exactly: **27 entities, 22 resources, 18 rules, 20 operations, 12 workflows**.
- Do not rename or expand the canonical catalog to match the incorrect V1 reference documentation; the catalog/spec are authoritative.
- Keep `project.review_change` canonical and `project.prepare_change_review` legacy-only.
- `project.prepare_commit` must never execute `git commit`, fabricate a commit hash, or claim an unverified commit occurred.
- Generic Project must remain usable without software/repository resources.
- Software capability activation must require grounded canonical/shared evidence; arbitrary strings, suffix collisions, booleans, mappings or caller-created objects are not authority.
- Project → Life Plan contributions require runtime-owned, verifiable authorization evidence and purpose minimization.
- Formation remains outside Project.
- Project must reuse shared planner/development/execution/validation/permission/approval/transaction/rollback/memory/trace infrastructure. Do not create parallel Project-local infrastructure.
- AT-DP-030 remains **56 connected checkpoints**. Rework checkpoint content, not the count.
- Permanent adversarial closure gate remains **34 top-level attack classes**. Strengthen existing attack classes with the new probes instead of adding cosmetic extra classes.
- Preserve all V1 audit evidence and historical audit bundles. Never stage any `.tar.gz`.
- Candidate documentation must keep `DP_030=REQUIRES_PHASE_INSPECTION` and `INDEPENDENT_REAUDIT_V2=PENDING`.
- No push. No merge.
- A V2 audit bundle must be produced from committed `HEAD` using `git archive`; stop after producing it for independent re-audit.

---

## Finding Map

```text
B1  Project → Life Plan authorization bypass
B2  caller-controlled committed-state forgery
M1  Project bootstrap does not extend frozen 10.29 chain
M2  self-development E2E does not execute shared runtime
M3  independent trace inventory/validation not demonstrated
M4  canonical reference documentation diverges from catalog
M5  software capability activation accepts ungrounded caller primitives
```

## Files Expected to Change

Production:

```text
cmm/domains/project/__init__.py
cmm/domains/project/bootstrap.py
cmm/domains/project/operations.py
cmm/domains/project/permissions.py
cmm/domains/project/profile.py
cmm/domains/project/rules.py
cmm/domains/project/trace.py
```

Tests:

```text
tests/domains/test_project_domain_bootstrap.py
tests/domains/test_project_domain_closure_adversarial.py
tests/domains/test_project_domain_cross_domain.py
tests/domains/test_project_domain_dp030_acceptance.py
tests/domains/test_project_domain_operations.py
tests/domains/test_project_domain_profile.py
tests/domains/test_project_domain_public_api.py
tests/domains/test_project_domain_self_development_e2e.py
tests/domains/test_project_domain_trace.py
```

Documentation:

```text
docs/reference/project-domain.md
docs/roadmap/phase-10-domain-intelligence.md
docs/reference/domain-intelligence-requirements-matrix.md
ROADMAP.md
```

Shared files may be modified only if a RED regression proves that the shared contract itself cannot express the frozen design. Any such change must be minimal, generic, separately committed, and covered by shared tests.

---

### Task 0: Baseline Guard and Audit Contract Freeze

**Files:**
- Verify: `docs/audits/phase-10.30-project-independent-audit-v1.md`
- Verify: `docs/superpowers/specs/2026-08-26-project-domain-design.md`
- Verify: `docs/superpowers/plans/2026-08-26-project-domain-implementation.md`
- Verify: this remediation plan.

**Interfaces:**
- Consumes: audit commit `ee67300`.
- Produces: clean tracked baseline and frozen remediation acceptance matrix.

- [ ] **Step 1: Verify branch, lineage and clean tracked state**

```bash
cd "/Users/chris/CMM OS" || exit 1
set -euo pipefail

test "$(git branch --show-current)" = "feature/phase-10-domain-intelligence"
git merge-base --is-ancestor ee67300 HEAD
test -z "$(git diff --name-only)"
test -z "$(git diff --cached --name-only)"
git status --short --branch
```

Expected: only historical audit bundles and `phase-10.30-audit-v1.tar.gz` may be untracked.

- [ ] **Step 2: Read all three authoritative documents completely**

```bash
cat docs/superpowers/specs/2026-08-26-project-domain-design.md
cat docs/superpowers/plans/2026-08-26-project-domain-implementation.md
cat docs/audits/phase-10.30-project-independent-audit-v1.md
```

Expected: all seven findings and their acceptance requirements are understood before production edits.

- [ ] **Step 3: Freeze current structural counts**

```bash
test "$(find cmm/domains/project -maxdepth 1 -type f -name '*.py' | wc -l | tr -d ' ')" = "14"
.venv/bin/python - <<'PY'
from cmm.domains.project.catalog import (
    CANONICAL_PROJECT_ENTITY_IDS,
    CANONICAL_PROJECT_RESOURCE_IDS,
    CANONICAL_PROJECT_RULE_IDS,
    CANONICAL_PROJECT_OPERATION_IDS,
    CANONICAL_PROJECT_WORKFLOW_IDS,
)
assert tuple(map(len, (
    CANONICAL_PROJECT_ENTITY_IDS,
    CANONICAL_PROJECT_RESOURCE_IDS,
    CANONICAL_PROJECT_RULE_IDS,
    CANONICAL_PROJECT_OPERATION_IDS,
    CANONICAL_PROJECT_WORKFLOW_IDS,
))) == (27, 22, 18, 20, 12)
PY
```

Expected: PASS.

---

### Task 1: B1 — Enforce Real Project → Life Plan Authorization

**Files:**
- Modify: `cmm/domains/project/rules.py`
- Modify: `cmm/domains/project/permissions.py` only as required by the real shared authorization path.
- Modify: `cmm/domains/project/__init__.py`
- Test: `tests/domains/test_project_domain_cross_domain.py`
- Test: `tests/domains/test_project_domain_closure_adversarial.py`

**Interfaces:**
- Consumes: existing shared/Life Plan cross-domain permission/trust contracts.
- Produces: `authorize_project_life_plan_contribution(...)` and an authorization-gated Project → Life Plan contribution path.

- [ ] **Step 1: Inspect the hardened existing cross-domain authorization pattern**

```bash
rg -n \
  'authorize_.*life_plan|cross_domain.*author|DomainCross|permission.*evidence|target_domain.*life' \
  cmm tests/domains
```

Read the complete matching implementation and tests before editing Project.

- [ ] **Step 2: Write RED tests proving raw projection is not authorization**

Add tests equivalent to:

```python
def test_project_life_plan_contribution_requires_runtime_authorization():
    raw = {
        "project_status_impact": "active",
        "timeline_impact": "Q4",
    }

    with pytest.raises((PermissionError, ValueError)):
        authorize_project_life_plan_contribution(
            raw,
            authorization_evidence=None,
        )


def test_project_life_plan_contribution_rejects_caller_forged_evidence():
    forged = {
        "is_authorized": True,
        "source_domain": "domain:project",
        "target_domain": "domain:life-plan",
    }

    with pytest.raises((PermissionError, ValueError, TypeError)):
        authorize_project_life_plan_contribution(
            {"project_status_impact": "active"},
            authorization_evidence=forged,
        )
```

Use the exact shared evidence type discovered in Step 1; do not invent a Project-specific token type.

- [ ] **Step 3: Run focused RED**

```bash
.venv/bin/python -m pytest -q \
  tests/domains/test_project_domain_cross_domain.py \
  tests/domains/test_project_domain_closure_adversarial.py
```

Expected: FAIL because the real authorization API/verification path is absent or accepts forged evidence.

- [ ] **Step 4: Implement the minimal authorization-gated contribution path**

The implementation must:

```text
1. validate runtime-owned permission/gate evidence;
2. bind source domain:project;
3. bind target domain:life-plan;
4. bind actor/session/purpose/context when present in the shared contract;
5. reject DENY/missing/forged/stale/mismatched evidence;
6. purpose-minimize the payload;
7. only then construct the contribution/projection.
```

`build_project_life_plan_projection(...)` may remain a pure minimizer, but it must not be used as proof of authorization. The public cross-domain path must pass through the real authorizer.

- [ ] **Step 5: Add adversarial regressions inside the existing cross-domain attack class**

Cover at least:

```text
missing evidence
raw mapping pretending to be evidence
wrapped caller-created evidence
source mismatch
target mismatch
purpose/context mismatch
DENY result
stale evidence if the shared contract exposes temporal validity
```

Keep 34 top-level adversarial functions.

- [ ] **Step 6: Run GREEN and shared regressions**

```bash
.venv/bin/python -m pytest -q \
  tests/domains/test_project_domain_cross_domain.py \
  tests/domains/test_project_domain_closure_adversarial.py \
  tests/domains/test_cross_domain_engine.py
```

Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add -- \
  cmm/domains/project/rules.py \
  cmm/domains/project/permissions.py \
  cmm/domains/project/__init__.py \
  tests/domains/test_project_domain_cross_domain.py \
  tests/domains/test_project_domain_closure_adversarial.py
git diff --cached --check
git commit -m "fix(project): enforce life plan authorization boundary"
```

---

### Task 2: B2 — Make Commit State Runtime-Owned

**Files:**
- Modify: `cmm/domains/project/operations.py`
- Modify: `cmm/domains/project/__init__.py` if public signatures change.
- Test: `tests/domains/test_project_domain_operations.py`
- Test: `tests/domains/test_project_domain_closure_adversarial.py`

**Interfaces:**
- Consumes: validation result, CommitGate result, approval evidence.
- Produces: commit-readiness result that cannot claim `committed=True` from caller data.

- [ ] **Step 1: Write RED tests for forged commit references**

```python
def test_prepare_commit_never_treats_raw_reference_as_commit_authority():
    result = build_prepare_commit_readiness_result(
        validation_passed=False,
        commit_gate_allowed=False,
        approval_reference=None,
        authoritative_commit_reference="caller:fake",
    )
    assert result["ready_for_approved_commit"] is False
    assert result["committed"] is False
```

Add the same assertion for:

```text
validation failed + fake reference
gate denied + fake reference
missing approval + fake reference
mismatched repository/change identity
caller-created wrapped "commit outcome"
```

- [ ] **Step 2: Run focused RED**

```bash
.venv/bin/python -m pytest -q \
  tests/domains/test_project_domain_operations.py \
  tests/domains/test_project_domain_closure_adversarial.py
```

Expected: existing implementation exposes the forgery.

- [ ] **Step 3: Implement the minimal safe state model**

Preferred remediation:

```text
project.prepare_commit
→ ready_for_approved_commit only
→ committed always False
```

unless an existing shared runtime-owned commit outcome contract already exists and can be verified without adding Project-local Git infrastructure.

A plain string, boolean, arbitrary mapping or caller-created object must never prove commit completion.

- [ ] **Step 4: Verify `prepare_commit` cannot mutate Git**

Use a temporary repository test:

```python
head_before = git_head(repo)
result = build_prepare_commit_readiness_result(...)
head_after = git_head(repo)

assert head_after == head_before
assert result["committed"] is False
```

- [ ] **Step 5: Run GREEN**

```bash
.venv/bin/python -m pytest -q \
  tests/domains/test_project_domain_operations.py \
  tests/domains/test_project_domain_closure_adversarial.py
```

Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add -- \
  cmm/domains/project/operations.py \
  cmm/domains/project/__init__.py \
  tests/domains/test_project_domain_operations.py \
  tests/domains/test_project_domain_closure_adversarial.py
git diff --cached --check
git commit -m "fix(project): prevent forged committed state"
```

---

### Task 3: M5 — Ground Software Capability Activation

**Files:**
- Modify: `cmm/domains/project/profile.py`
- Test: `tests/domains/test_project_domain_profile.py`
- Test: `tests/domains/test_project_domain_closure_adversarial.py`
- Test: `tests/domains/test_project_domain_dp030_acceptance.py` if setup helpers require grounded context.

**Interfaces:**
- Consumes: exact canonical registered workflow/operation/resource evidence or verified repository-backed shared context.
- Produces: fail-closed `project_software_capability_active(...)`.

- [ ] **Step 1: Write RED forgery tests**

```python
def test_fake_software_workflow_prefix_does_not_activate():
    assert project_software_capability_active(
        workflow_id="project.software_forged"
    ) is False


def test_resource_suffix_collision_does_not_activate():
    assert project_software_capability_active(
        resource_ids=("attacker.source_code",)
    ) is False


def test_raw_repository_boolean_is_not_authority():
    assert project_software_capability_active(
        repository_backed=True
    ) is False
```

Preserve positive tests for actual canonical software signals.

- [ ] **Step 2: Run RED**

```bash
.venv/bin/python -m pytest -q \
  tests/domains/test_project_domain_profile.py \
  tests/domains/test_project_domain_closure_adversarial.py
```

Expected: forged primitives currently activate.

- [ ] **Step 3: Implement exact-match/verified activation**

Allowed activation evidence:

```text
exact canonical software workflow ID
exact canonical software operation ID
resolved canonical Project software resource
verified repository-backed shared goal/context
resolved canonical capability
```

Remove prefix activation and suffix matching on untrusted strings. If a repository-backed boolean remains for backward compatibility, it cannot independently activate software semantics.

- [ ] **Step 4: Run GREEN**

```bash
.venv/bin/python -m pytest -q \
  tests/domains/test_project_domain_profile.py \
  tests/domains/test_project_domain_rules.py \
  tests/domains/test_project_domain_closure_adversarial.py
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add -- \
  cmm/domains/project/profile.py \
  tests/domains/test_project_domain_profile.py \
  tests/domains/test_project_domain_closure_adversarial.py \
  tests/domains/test_project_domain_dp030_acceptance.py
git diff --cached --check
git commit -m "fix(project): ground software capability activation"
```

---

### Task 4: M1 — Extend the Standard Bootstrap Through Life Plan

**Files:**
- Modify: `cmm/domains/project/bootstrap.py`
- Test: `tests/domains/test_project_domain_bootstrap.py`
- Test: `tests/domains/test_project_domain_public_api.py`

**Interfaces:**
- Consumes: `build_standard_life_plan_domain_bootstrap()`.
- Produces: standard Project bootstrap using the same registry objects and preserving prior domains/fallback.

- [ ] **Step 1: Write RED bootstrap tests**

```python
def test_project_bootstrap_extends_life_plan_chain():
    bootstrap = build_standard_project_domain_bootstrap()

    assert bootstrap.domain_registry.get("domain:general") is not None
    assert bootstrap.domain_registry.get("domain:life-plan") is not None
    assert bootstrap.domain_registry.get("domain:project") is not None


def test_project_bootstrap_does_not_register_future_domains():
    bootstrap = build_standard_project_domain_bootstrap()

    assert bootstrap.domain_registry.get("domain:mental-health") is None
    assert bootstrap.domain_registry.get("domain:neurodivergence") is None
```

Also assert the same registries from the Life Plan bootstrap are extended rather than copied into parallel stores, using the concrete bootstrap contract.

- [ ] **Step 2: Run RED**

```bash
.venv/bin/python -m pytest -q \
  tests/domains/test_project_domain_bootstrap.py \
  tests/domains/test_project_domain_public_api.py \
  tests/domains/test_life_plan_domain_bootstrap.py
```

Expected: current General-only Project bootstrap fails the Life Plan preservation assertion.

- [ ] **Step 3: Replace General-only bootstrap with the 10.29 chain**

Required structure:

```python
prior = build_standard_life_plan_domain_bootstrap()

register_project_domain(
    domain_registry=prior.domain_registry,
    profile_registry=prior.profile_registry,
    resource_registry=prior.resource_registry,
    rule_registry=prior.rule_registry,
    operation_registry=prior.operation_registry,
    workflow_registry=prior.workflow_registry,
    permission_registry=prior.permission_registry,
)
```

Adapt field names to the exact existing Life Plan bootstrap dataclass.

- [ ] **Step 4: Run GREEN + fallback regressions**

```bash
.venv/bin/python -m pytest -q \
  tests/domains/test_project_domain_bootstrap.py \
  tests/domains/test_project_domain_public_api.py \
  tests/domains/test_general_domain_bootstrap.py \
  tests/domains/test_life_plan_domain_bootstrap.py
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add -- \
  cmm/domains/project/bootstrap.py \
  tests/domains/test_project_domain_bootstrap.py \
  tests/domains/test_project_domain_public_api.py
git diff --cached --check
git commit -m "fix(project): extend standard bootstrap through life plan"
```

---

### Task 5: M3 — Build and Validate Independent Trace Inventory

**Files:**
- Modify: `cmm/domains/project/trace.py` only if a small adapter is needed.
- Test: `tests/domains/test_project_domain_trace.py`
- Test: `tests/domains/test_project_domain_dp030_acceptance.py`
- Test: `tests/domains/test_project_domain_closure_adversarial.py`

**Interfaces:**
- Consumes: runtime/expected reference artifacts built before trace assembly.
- Produces: independent `DomainTraceReferenceInventory` → trace assembly → `validate_project_trace(...)`.

- [ ] **Step 1: Inspect hardened Life Plan trace pattern**

```bash
rg -n \
  'DomainTraceReferenceInventory|calculate_domain_trace_identity|validate_.*trace' \
  cmm/domains/life_plan tests/domains/test_life_plan_domain_*.py
```

Read the complete matching implementation/test sequence.

- [ ] **Step 2: Write RED trace tests**

The test must explicitly perform:

```python
inventory = DomainTraceReferenceInventory(...)
trace = assemble_project_trace(...)
validate_project_trace(trace, expected_inventory=inventory)
```

The `inventory` must exist before `trace`.

Add tamper cases:

```text
missing expected reference
extra trace reference
substituted forged reference
inventory constructed from final trace
identity mismatch
```

- [ ] **Step 3: Run RED**

```bash
.venv/bin/python -m pytest -q \
  tests/domains/test_project_domain_trace.py \
  tests/domains/test_project_domain_closure_adversarial.py
```

Expected: current acceptance/adversarial coverage is insufficient.

- [ ] **Step 4: Implement/reuse the hardened trace path**

Use existing:

```text
calculate_domain_trace_identity(...)
DomainTraceReferenceInventory
DomainTraceAssembler
shared validators
```

No Project-local trace store or probe.

- [ ] **Step 5: Run GREEN**

```bash
.venv/bin/python -m pytest -q \
  tests/domains/test_project_domain_trace.py \
  tests/domains/test_project_domain_closure_adversarial.py \
  tests/domains/test_life_plan_domain_trace.py
```

Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add -- \
  cmm/domains/project/trace.py \
  tests/domains/test_project_domain_trace.py \
  tests/domains/test_project_domain_dp030_acceptance.py \
  tests/domains/test_project_domain_closure_adversarial.py
git diff --cached --check
git commit -m "fix(project): validate independent trace inventory"
```

---

### Task 6: M2 — Execute Real Shared Self-Development Runtime

**Files:**
- Modify: `tests/domains/test_project_domain_self_development_e2e.py`
- Modify: `tests/domains/test_project_domain_dp030_acceptance.py`
- Modify production only if the real shared path exposes a proven wiring defect.

**Interfaces:**
- Consumes: existing `cmm/development/`, repository analysis/context, shared planning, permission/approval, semantic mutation, transaction/rollback and Phase 7 Validation.
- Produces: a genuine temporary-repository self-development E2E.

- [ ] **Step 1: Read the existing shared self-development/runtime tests**

```bash
rg -n \
  'tmp_path|ProjectAnalyzer|ProjectContext|rollback|transaction|Validation|semantic.*operation|modify.*code' \
  tests cmm/development cmm/execution cmm/transformations cmm/validation
```

Read the complete nearest working E2E before composing Project.

- [ ] **Step 2: Replace synthetic E2E fixtures with a real temporary Git/Python repository**

The test setup must:

```bash
git init
git config user.email test@example.com
git config user.name "CMM Test"
```

and create a minimal Python module plus a test.

Capture:

```text
initial Git HEAD
initial source bytes
```

- [ ] **Step 3: Execute real shared repository observation and planning**

Assertions must be based on runtime outputs, not catalog existence:

```text
repository observed through existing ProjectAnalyzer/ProjectContext or canonical shared equivalent
implementation plan produced through the existing shared planning/development contract
```

- [ ] **Step 4: Execute permissioned controlled mutation**

Inject `project.modify_code` through the shared operation registry using the existing controlled semantic mutation path.

Required negative path:

```text
no approval → no mutation
```

Required positive path:

```text
valid approval → controlled reversible mutation
```

- [ ] **Step 5: Execute real transaction/rollback**

Force a downstream failure and prove:

```text
source bytes restored
working tree restored according to the shared rollback contract
no false committed state
```

- [ ] **Step 6: Execute real Phase 7 validation**

Do not use sentinel strings such as:

```text
"validation:phase7:pass"
```

Run the actual shared validation pipeline/validator used by the repository and assert a real validation result object/report.

Exercise both:

```text
failed validation → not ready
successful validation → ready_for_approved_commit
```

- [ ] **Step 7: Prove `prepare_commit` does not move Git HEAD**

```python
assert git_head(repo) == head_before_prepare_commit
assert readiness["ready_for_approved_commit"] is True
assert readiness["committed"] is False
```

- [ ] **Step 8: Bind real outputs into Project memory/trace evidence**

Memory proposal and trace references must point to the actual repository observation, plan, mutation, validation and readiness artifacts produced by the E2E.

- [ ] **Step 9: Run focused GREEN**

```bash
.venv/bin/python -m pytest -q \
  tests/domains/test_project_domain_self_development_e2e.py \
  tests/domains/test_project_domain_operations.py \
  tests/domains/test_project_domain_permissions.py \
  tests/domains/test_project_domain_trace.py
```

Expected: PASS.

- [ ] **Step 10: Commit**

```bash
git add -- \
  tests/domains/test_project_domain_self_development_e2e.py \
  tests/domains/test_project_domain_dp030_acceptance.py
git diff --cached --check
git commit -m "test(project): execute shared self development runtime"
```

If a production/shared wiring fix was proven necessary, stage it in its own preceding commit with its RED test.

---

### Task 7: Rebuild AT-DP-030 as a Genuine 56-Checkpoint Connected Acceptance

**Files:**
- Modify: `tests/domains/test_project_domain_dp030_acceptance.py`

**Interfaces:**
- Consumes: repaired authorization, commit readiness, grounded software activation, 10.29 bootstrap, real self-development runtime and independent trace inventory.
- Produces: exactly 56 connected checkpoints whose labels match executed behavior.

- [ ] **Step 1: Preserve exactly 56 checkpoint calls**

```bash
python3 - <<'PY'
from pathlib import Path
p = Path("tests/domains/test_project_domain_dp030_acceptance.py")
text = p.read_text()
print(text.count("checkpoint("))
PY
```

Expected before/after remediation: 56 using the file's actual checkpoint helper/call syntax.

- [ ] **Step 2: Repair the cross-domain segment**

The connected artifact flow must be:

```text
generic project state
→ real shared Project permission/trust authorization
→ purpose-minimized Project contribution
→ Life Plan consumer
```

The checkpoint must not record `DENY` and then bypass it.

- [ ] **Step 3: Repair the trace segment**

The connected flow must be:

```text
runtime artifacts
→ independent preassembly reference inventory
→ shared trace identity
→ final trace assembly
→ validate_project_trace(...)
```

- [ ] **Step 4: Repair the software/self-development segment**

Checkpoint labels claiming repository observation, planning, mutation, rollback and validation must carry the real artifacts from Task 6 or execute the same shared path in the connected test. Catalog-definition existence is insufficient.

- [ ] **Step 5: Repair commit readiness checkpoints**

Assert:

```text
failed validation → ready=False, committed=False
successful validation + gate + approval → ready=True, committed=False
Git HEAD unchanged
```

- [ ] **Step 6: Run AT-DP-030**

```bash
.venv/bin/python -m pytest -q tests/domains/test_project_domain_dp030_acceptance.py
```

Expected: PASS with exactly 56 connected checkpoints.

- [ ] **Step 7: Commit**

```bash
git add -- tests/domains/test_project_domain_dp030_acceptance.py
git diff --cached --check
git commit -m "test(project): repair connected AT-DP-030 acceptance"
```

---

### Task 8: Harden the Existing 34-Class Permanent Adversarial Gate

**Files:**
- Modify: `tests/domains/test_project_domain_closure_adversarial.py`

**Interfaces:**
- Consumes: all repaired trust boundaries.
- Produces: exactly 34 top-level `test_attack_*` classes/functions with materially stronger probes.

- [ ] **Step 1: Verify current top-level attack count**

```bash
python3 - <<'PY'
import ast
from pathlib import Path
tree = ast.parse(Path("tests/domains/test_project_domain_closure_adversarial.py").read_text())
tests = [
    n.name for n in tree.body
    if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef))
    and n.name.startswith("test_attack_")
]
print(len(tests))
assert len(tests) == 34
PY
```

- [ ] **Step 2: Strengthen existing attack classes without cosmetic count inflation**

Ensure the 34 functions collectively cover:

```text
B1 missing/forged/mismatched Project→Life Plan authorization
B2 fake commit reference and false committed state
M5 forged workflow/resource/repository signals
M3 trace-inventory derivation/tamper/substitution
M1 future-domain bootstrap isolation
M2 approval bypass, mutation bypass, rollback bypass, fake validation
parallel Project-local infrastructure rejection
memory confirmation bypass
legacy operation collision
Formation absorption
generic Project forced into software
```

Use multiple assertions/subcases inside the relevant existing top-level attack functions.

- [ ] **Step 3: Run the gate**

```bash
.venv/bin/python -m pytest -q tests/domains/test_project_domain_closure_adversarial.py
```

Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add -- tests/domains/test_project_domain_closure_adversarial.py
git diff --cached --check
git commit -m "test(project): close audit v1 adversarial gaps"
```

---

### Task 9: M4 — Synchronize Canonical Reference Documentation from Catalog

**Files:**
- Modify: `docs/reference/project-domain.md`
- Modify: `docs/roadmap/phase-10-domain-intelligence.md`
- Modify: `docs/reference/domain-intelligence-requirements-matrix.md`
- Modify: `ROADMAP.md`
- Test: add or extend an existing Project documentation/catalog consistency test in `tests/domains/test_project_domain_catalog.py`.

**Interfaces:**
- Consumes: `cmm/domains/project/catalog.py`.
- Produces: documentation that exactly names the implemented canonical inventory and audit-pending status.

- [ ] **Step 1: Write RED consistency assertions**

Use the actual documentation format. The test must parse/check the documented canonical IDs against:

```python
CANONICAL_PROJECT_ENTITY_IDS
CANONICAL_PROJECT_RESOURCE_IDS
CANONICAL_PROJECT_RULE_IDS
CANONICAL_PROJECT_OPERATION_IDS
CANONICAL_PROJECT_WORKFLOW_IDS
```

At minimum, every canonical ID must appear exactly in the canonical inventory section and known V1 noncanonical aliases must not be presented as canonical.

- [ ] **Step 2: Run RED**

```bash
.venv/bin/python -m pytest -q tests/domains/test_project_domain_catalog.py
```

Expected: current reference inventory diverges.

- [ ] **Step 3: Rewrite canonical inventory from `catalog.py`**

Do not change catalog IDs to match documentation.

Document:

```text
14 modules
27 entities
22 resources
18 rules
20 operations
12 workflows
AT-DP-030 56 checkpoints
adversarial 34 attack classes
independent audit V1 FAIL
remediation implemented
independent re-audit V2 pending
DP-030 still REQUIRES_PHASE_INSPECTION
```

- [ ] **Step 4: Run consistency test and documentation scans**

```bash
.venv/bin/python -m pytest -q tests/domains/test_project_domain_catalog.py

rg -n \
  'DP-030|AT-DP-030|10\.30|domain:project|ProjectProfile|independent.*audit|re-audit' \
  docs/reference/project-domain.md \
  docs/roadmap/phase-10-domain-intelligence.md \
  docs/reference/domain-intelligence-requirements-matrix.md \
  ROADMAP.md

git diff --check
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add -- \
  docs/reference/project-domain.md \
  docs/roadmap/phase-10-domain-intelligence.md \
  docs/reference/domain-intelligence-requirements-matrix.md \
  ROADMAP.md \
  tests/domains/test_project_domain_catalog.py
git diff --cached --check
git commit -m "docs(project): align canonical reference with catalog"
```

---

### Task 10: Full Verification and Audit Bundle V2

**Files:**
- No new production architecture.
- Audit output: untracked `phase-10.30-audit-v2.tar.gz`.

**Interfaces:**
- Consumes: all remediation commits.
- Produces: clean, committed V2 candidate for independent re-audit.

- [ ] **Step 1: Verify exact structure and forbidden infrastructure**

```bash
test "$(find cmm/domains/project -maxdepth 1 -type f -name '*.py' | wc -l | tr -d ' ')" = "14"

! rg -n \
  'subprocess|os\.system|shell=True|git commit|ProjectMemoryStore|ProjectPlanner|ProjectAgentRuntime|ProjectWorkflowEngine|ProjectTraceStore|ProjectPermissionEngine|ProjectGitRuntime' \
  cmm/domains/project

! rg -n 'domain-trace:probe' cmm/domains/project
```

Expected: PASS.

- [ ] **Step 2: Verify frozen catalog counts**

```bash
.venv/bin/python - <<'PY'
from cmm.domains.project.catalog import (
    CANONICAL_PROJECT_ENTITY_IDS,
    CANONICAL_PROJECT_RESOURCE_IDS,
    CANONICAL_PROJECT_RULE_IDS,
    CANONICAL_PROJECT_OPERATION_IDS,
    CANONICAL_PROJECT_WORKFLOW_IDS,
)
counts = tuple(map(len, (
    CANONICAL_PROJECT_ENTITY_IDS,
    CANONICAL_PROJECT_RESOURCE_IDS,
    CANONICAL_PROJECT_RULE_IDS,
    CANONICAL_PROJECT_OPERATION_IDS,
    CANONICAL_PROJECT_WORKFLOW_IDS,
)))
assert counts == (27, 22, 18, 20, 12), counts
print(counts)
PY
```

- [ ] **Step 3: Verify exact acceptance/adversarial counts**

```bash
.venv/bin/python - <<'PY'
import ast
from pathlib import Path

adv = Path("tests/domains/test_project_domain_closure_adversarial.py")
tree = ast.parse(adv.read_text())
attacks = [
    n.name for n in tree.body
    if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef))
    and n.name.startswith("test_attack_")
]
assert len(attacks) == 34, len(attacks)

accept = Path("tests/domains/test_project_domain_dp030_acceptance.py").read_text()
checkpoint_count = accept.count("checkpoint(")
assert checkpoint_count == 56, checkpoint_count

print("ATTACK_CLASSES=34")
print("AT_DP_030_CHECKPOINT_CALLS=56")
PY
```

If the file uses a differently named checkpoint helper, count the exact established helper rather than forcing this literal.

- [ ] **Step 4: Run Project suite**

```bash
.venv/bin/python -m pytest -q tests/domains/test_project_domain_*.py
```

Record exact observed count.

- [ ] **Step 5: Run Domain suite**

```bash
.venv/bin/python -m pytest -q tests/domains
```

Record exact observed count.

- [ ] **Step 6: Run global suite**

```bash
.venv/bin/python -m pytest -q
```

Record exact observed count.

- [ ] **Step 7: Static verification**

```bash
.venv/bin/ruff check cmm/domains/project tests/domains/test_project_domain_*.py
.venv/bin/ruff format --check cmm/domains/project tests/domains/test_project_domain_*.py
.venv/bin/python -m compileall -q cmm/domains/project
git diff --check
```

Expected: PASS.

- [ ] **Step 8: Verify remediation acceptance matrix explicitly**

Print and prove:

```text
B1_PROJECT_LIFE_PLAN_RUNTIME_AUTHORIZATION=PASS
B1_RAW_MAPPING_NOT_AUTHORITY=PASS
B1_FORGED_PERMISSION_EVIDENCE_REJECTED=PASS
B1_CONTEXT_PURPOSE_TARGET_MISMATCH_REJECTED=PASS

B2_CALLER_COMMIT_REFERENCE_NOT_AUTHORITY=PASS
B2_FAILED_VALIDATION_CANNOT_COMMIT=PASS
B2_DENIED_GATE_CANNOT_COMMIT=PASS
B2_MISSING_APPROVAL_CANNOT_COMMIT=PASS
B2_COMMIT_OUTCOME_RUNTIME_OWNED=PASS

M1_PROJECT_BOOTSTRAP_EXTENDS_10_29=PASS
M1_GENERAL_FALLBACK_PRESERVED=PASS

M2_TEMP_REPOSITORY_SELF_DEVELOPMENT=PASS
M2_SHARED_REPOSITORY_ANALYSIS=PASS
M2_SHARED_PLANNING=PASS
M2_CONTROLLED_MUTATION=PASS
M2_TRANSACTION_ROLLBACK=PASS
M2_PHASE7_VALIDATION_REAL=PASS
M2_PREPARE_COMMIT_HEAD_UNCHANGED=PASS

M3_TRACE_INVENTORY_PREASSEMBLY=PASS
M3_TRACE_VALIDATION_REAL=PASS
M3_TRACE_TAMPER_REJECTED=PASS

M4_REFERENCE_INVENTORY_MATCHES_CATALOG=PASS
M4_AGENT_REPORT_GENERATED_FROM_CATALOG=PASS

M5_FAKE_WORKFLOW_ACTIVATION_REJECTED=PASS
M5_SUFFIX_COLLISION_RESOURCE_REJECTED=PASS
M5_REPOSITORY_SIGNAL_GROUNDED=PASS
```

Every line must correspond to an executed test/assertion, not a manually asserted echo.

- [ ] **Step 9: Verify clean tracked state**

```bash
test -z "$(git diff --name-only)"
test -z "$(git diff --cached --name-only)"
git status --short --branch
```

Expected: only historical bundles plus V1 bundle are untracked before V2 generation.

- [ ] **Step 10: Generate V2 from committed HEAD**

```bash
rm -f phase-10.30-audit-v2.tar.gz

git archive \
  --format=tar.gz \
  --output=phase-10.30-audit-v2.tar.gz \
  HEAD

tar -tzf phase-10.30-audit-v2.tar.gz \
  | grep -q '^cmm/domains/project/catalog.py$'

tar -tzf phase-10.30-audit-v2.tar.gz \
  | grep -q '^docs/audits/phase-10.30-project-independent-audit-v1.md$'

tar -tzf phase-10.30-audit-v2.tar.gz \
  | grep -q '^tests/domains/test_project_domain_dp030_acceptance.py$'

shasum -a 256 phase-10.30-audit-v2.tar.gz
```

The V2 bundle remains untracked.

- [ ] **Step 11: Stop at the independent re-audit boundary**

Final candidate state:

```text
PHASE10_30_REMEDIATION_V1=COMPLETE
DP_030=REQUIRES_PHASE_INSPECTION
AT_DP_030=PASS
AT_DP_030_CHECKPOINTS=56
PROJECT_CLOSURE_ADVERSARIAL_GATE=PASS
PROJECT_CLOSURE_ATTACK_CLASSES=34
INDEPENDENT_AUDIT_V1=FAIL_RECORDED
INDEPENDENT_REAUDIT_V2=PENDING
AUDIT_BUNDLE=phase-10.30-audit-v2.tar.gz
PUSH=NO
MERGE=NO
NEXT=INDEPENDENT_REAUDIT_V2
```

Do not mark `DP_030=VERIFIED_EXISTING` and do not create a final closure commit.

---

## Self-Review Checklist

Before execution, confirm:

```text
B1 mapped to Task 1 + Tasks 7/8
B2 mapped to Task 2 + Tasks 7/8
M5 mapped to Task 3 + Tasks 7/8
M1 mapped to Task 4
M3 mapped to Task 5 + Tasks 7/8
M2 mapped to Task 6 + Task 7
M4 mapped to Task 9
full verification + V2 mapped to Task 10
```

No placeholder implementation instructions are permitted. If a shared contract must change, prove it with RED coverage and make the smallest generic change possible.
