# Phase 10.49 — Re-audit V2 → V3 Remediation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remove the two unreachable unconditional first-party requirements identified by Independent Re-audit V2, strengthen canonical reachability acceptance, correct the three remaining documentation/evidence defects, and produce a new exact-HEAD V3 re-audit bundle.

**Architecture:** Preserve the Phase 10.49 contracts, composition, validation, DomainDefinition/Domain Pack integration, canonical Phase 8 `KnowledgePackage`, canonical `KnowledgePackageBuilder`, and existing `DefaultDomainCognitiveIntegrator` seam. Production remediation is limited to the Concerns and Life Plan first-party schema declarations; all other changes are tests, status/reference documentation, gate evidence, and exact-HEAD audit packaging.

**Tech Stack:** Python, pytest, existing Phase 10.49 immutable schema contracts, canonical `cmm.cognitive` package/builder infrastructure, Ruff 0.16.2 baseline-aware gate, `compileall`, Git.

**Spec:** `docs/superpowers/specs/2026-09-11-phase-10.49-reaudit-v2-remediation-spec.md`

## Global Constraints

- Required branch: `feature/phase-10-domain-intelligence`.
- Required starting HEAD: `73d2e4501349194b038a4cb734c178baab1ec6b0`.
- Required V2 re-audit report: `docs/audits/phase-10.49-independent-reaudit-v2.md`.
- Required V2 re-audit report SHA-256: `729ea43478ba70f87c0d0a2ab164ed750e125463e33f98d6092e4e8e8a31d9cf`.
- Required V2 remediation spec SHA-256: `99205771fa858eec90bfb97804f84c0ea18aef87b9902ec3fc82374097123a21`.
- Preserve `MAJOR_01=VERIFIED_REMEDIATED` and `MAJOR_02=VERIFIED_REMEDIATED`.
- Preserve `UNIQUE_FIRST_PARTY_POLICY_SHAPES=12` where possible without artificial no-op differences.
- Preserve resolved-contradiction acceptance and no-mutation semantics.
- Preserve General observation-only compatibility.
- Preserve exactly 12 first-party schemas and do not create Mental Health or Neurodivergence schemas.
- Production scope is expected to be only:
  - `cmm/domains/concerns/knowledge_package.py`
  - `cmm/domains/life_plan/knowledge_package.py`
- Do not modify shared Phase 10.49 contracts, composition, validation, Cognitive integration, or Phase 8 unless a new RED test disproves the V2 audit premise.
- Do not modify `cmm/cognitive/knowledge_packages.py`.
- Do not add `active_goals` or `missing_information` request plumbing merely to rescue the V2 schema.
- Do not create any builder, registry, loader, resolver, store, runtime, engine, second Knowledge Package model, second privacy subsystem, or contradiction resolver.
- Do not implement Phase 10.50, 10.52, 10.53, or Phase 11.
- Follow `TEST RED → MINIMAL IMPLEMENTATION → GREEN → CONTROLLED REFACTOR → NEXT BLOCK`.
- Historical V1/V2 audit reports and V1/V2 bundles are immutable.
- Live pre-audit status after remediation must be `PHASE10_49=IMPLEMENTED_PENDING_INDEPENDENT_REAUDIT_V3`.
- Do not publish live `BLOCKERS=0`, `MAJORS=0`, `MINORS=0` before independent V3 audit.
- Use one coherent authoritative final test/gate evidence set across live docs.
- No `push` or `merge`.
- Do not use `git stash`, `stash pop/apply/drop`, `git reset`, `git clean`, or `git worktree`.
- Preserve the quarantine stash `quarantine: post-audit phase 10.32 uncommitted changes`.
- Final bundle must be generated with `git archive` from exact committed V3 HEAD.

---

## File Structure

### Production files expected to change

- `cmm/domains/concerns/knowledge_package.py`
  - Remove unreachable unconditional `missing_information` requirement while preserving Concerns-specific uncertainty/contradiction semantics.

- `cmm/domains/life_plan/knowledge_package.py`
  - Remove unreachable unconditional `active_goals` requirement while preserving Life Plan-specific constraints/preferences/uncertainty semantics.

### Test files expected to change

- `tests/domains/test_domain_knowledge_package_first_party.py`
  - Add canonical required-field reachability invariant across all 12 schemas.

- `tests/domains/test_domain_knowledge_package_dp049_acceptance.py`
  - Add real Concerns and Life Plan canonical-path acceptance and retain all V2-remediated scenarios.

A new `tests/domains/test_domain_knowledge_package_reachability.py` is allowed only if the reachability invariant becomes materially clearer as an isolated test unit. Prefer the existing first-party test.

### Documentation expected to change

- `docs/reference/domain-knowledge-packages.md`
- `docs/roadmap/phase-10-domain-intelligence.md`
- `docs/reference/domain-intelligence-requirements-matrix.md`
- `ROADMAP.md`

Conditional only if actual clause evidence changes:

- `docs/audits/domain-prompt-clause-coverage.md`
- `tests/domains/test_domain_prompt_clause_coverage.py`

### Historical files that must remain unchanged

- `docs/audits/phase-10.49-independent-audit-v1.md`
- `docs/audits/phase-10.49-independent-reaudit-v2.md`

---

### Task 1: Reproduce MAJOR-03 with a Canonical Required-Field Reachability RED Test

**Files:**
- Modify: `tests/domains/test_domain_knowledge_package_first_party.py`
- Read only: `cmm/cognitive/knowledge_packages.py`
- Read only: `cmm/domains/cognitive_integration_contracts.py`
- Read only: `cmm/domains/cognitive_integration.py`
- Read only: `cmm/domains/concerns/knowledge_package.py`
- Read only: `cmm/domains/life_plan/knowledge_package.py`

**Interfaces:**
- Consumes: real first-party schemas, real `KnowledgePackageRequest`, real `KnowledgePackageBuilder`, real `DomainCognitiveIntegrationRequest`, real integrator request mapping.
- Produces: one generalized RED invariant proving no first-party `required_non_empty=True` field is unreachable.

- [ ] **Step 1: Verify the immutable remediation baseline**

Run:

```bash
git branch --show-current
git rev-parse HEAD
git status --short
git stash list

shasum -a 256 \
  docs/audits/phase-10.49-independent-reaudit-v2.md

shasum -a 256 \
  docs/superpowers/specs/2026-09-11-phase-10.49-reaudit-v2-remediation-spec.md
```

Expected:

```text
branch=feature/phase-10-domain-intelligence
HEAD=73d2e4501349194b038a4cb734c178baab1ec6b0
worktree clean
quarantine stash present
V2 report SHA=729ea43478ba70f87c0d0a2ab164ed750e125463e33f98d6092e4e8e8a31d9cf
remediation spec SHA=99205771fa858eec90bfb97804f84c0ea18aef87b9902ec3fc82374097123a21
```

- [ ] **Step 2: Inspect exact canonical construction seams**

Read the real dataclass fields and builder mapping.

Use a Python probe rather than assumptions:

```bash
.venv/bin/python - <<'PY'
from dataclasses import fields

from cmm.cognitive.knowledge_packages import (
    KnowledgePackage,
    KnowledgePackageRequest,
)
from cmm.domains.cognitive_integration_contracts import (
    DomainCognitiveIntegrationRequest,
)

print("KnowledgePackage =", [field.name for field in fields(KnowledgePackage)])
print(
    "KnowledgePackageRequest =",
    [field.name for field in fields(KnowledgePackageRequest)],
)
print(
    "DomainCognitiveIntegrationRequest =",
    [field.name for field in fields(DomainCognitiveIntegrationRequest)],
)
PY

grep -nE \
  'KnowledgePackageRequest\(|active_goals|missing_information' \
  cmm/cognitive/knowledge_packages.py \
  cmm/domains/cognitive_integration.py \
  cmm/domains/cognitive_integration_contracts.py
```

Expected evidence:

```text
KnowledgePackage contains active_goals
KnowledgePackageRequest does not expose active_goals
DomainCognitiveIntegrationRequest does not expose missing_information
```

Confirm builder/integrator mapping from source before encoding the test helper.

- [ ] **Step 3: Add a reachability model in test code only**

In `tests/domains/test_domain_knowledge_package_first_party.py`, define a small pure helper describing canonical construction reachability based on inspected contracts.

Do not create production metadata or a registry.

The test helper may use explicit finite sets derived from real code, for example:

```python
BUILDER_REQUEST_FIELDS = {
    field.name
    for field in dataclasses.fields(KnowledgePackageRequest)
}

DOMAIN_INTEGRATION_REQUEST_FIELDS = {
    field.name
    for field in dataclasses.fields(DomainCognitiveIntegrationRequest)
}
```

For fields derived by `KnowledgePackageBuilder` from store/resources, encode only fields with existing real builder/test evidence.

Keep this helper local to tests.

- [ ] **Step 4: Add generalized RED test**

Add a test with a stable shape such as:

```python
def test_all_first_party_required_non_empty_fields_are_canonically_reachable() -> None:
    schemas = _all_first_party_schemas()

    unreachable: set[str] = set()

    for domain_name, schema in schemas.items():
        for policy in schema.field_policies:
            if not policy.required_non_empty:
                continue

            if not _is_canonically_reachable(
                domain_name=domain_name,
                field_name=policy.field_name,
            ):
                unreachable.add(f"{domain_name}.{policy.field_name}")

    assert unreachable == set()
```

Against V2, expected failing set must include:

```text
concerns.missing_information
life_plan.active_goals
```

Do not hard-code the test to only those two strings if a generalized invariant can expose future errors.

- [ ] **Step 5: Add exact diagnostic assertion text**

Use deterministic ordering in failure output:

```python
assert sorted(unreachable) == []
```

or an equivalent assertion that prints the complete offending set.

The RED output must be machine-auditable.

- [ ] **Step 6: Run the RED node**

Run:

```bash
.venv/bin/python -m pytest \
  tests/domains/test_domain_knowledge_package_first_party.py::test_all_first_party_required_non_empty_fields_are_canonically_reachable \
  -q
```

Required result:

```text
FAIL
```

and the failure must identify at least:

```text
concerns.missing_information
life_plan.active_goals
```

Record the exact output for the final agent handoff.

- [ ] **Step 7: Re-run already-verified V2 semantic guards before production changes**

Run the exact nodes that prove:

```text
first-party policy differentiation
General observation-only compatibility
resolved contradiction preservation
```

These should remain GREEN at the starting HEAD.

This establishes that the new remediation must not disturb V1 majors already verified in V2.

- [ ] **Step 8: Commit RED test only**

```bash
git add \
  tests/domains/test_domain_knowledge_package_first_party.py

git diff --cached --check

git commit -m \
  "test(domains): reproduce phase 10.49 v2 reachability finding"
```

No production file may be included.

---

### Task 2: Remove Only the Two Unreachable First-Party Requirements

**Files:**
- Modify: `cmm/domains/concerns/knowledge_package.py`
- Modify: `cmm/domains/life_plan/knowledge_package.py`
- Test: `tests/domains/test_domain_knowledge_package_first_party.py`

**Interfaces:**
- Consumes: existing immutable Phase 10.49 schema contracts.
- Produces: reachable Concerns/Life Plan declarations while preserving Domain-specific differentiation.

- [ ] **Step 1: Inspect the complete current V2 schemas**

Read:

```bash
sed -n '1,260p' cmm/domains/concerns/knowledge_package.py
sed -n '1,260p' cmm/domains/life_plan/knowledge_package.py
```

Identify only the policies containing:

```text
missing_information.required_non_empty=True
active_goals.required_non_empty=True
```

Do not rewrite unrelated policy bodies.

- [ ] **Step 2: Apply the minimal Concerns correction**

Remove the unconditional requirement:

```python
required_non_empty=True
```

from the `missing_information` field policy.

If the entire policy has no remaining semantic effect, remove that policy entry only.

If it carries a real non-mandatory Phase 10.49 semantic property supported by the contract, retain that property.

Do not add request plumbing.

- [ ] **Step 3: Apply the minimal Life Plan correction**

Remove:

```python
required_non_empty=True
```

from the `active_goals` field policy.

If the remaining field policy would be a no-op, remove the policy entry rather than keeping decorative configuration.

Retain Life Plan's other real constraints/preferences/uncertainty/provenance specialization.

Do not add request plumbing.

- [ ] **Step 4: Run the reachability test GREEN**

```bash
.venv/bin/python -m pytest \
  tests/domains/test_domain_knowledge_package_first_party.py::test_all_first_party_required_non_empty_fields_are_canonically_reachable \
  -q
```

Required:

```text
PASS
FIRST_PARTY_REQUIRED_FIELD_REACHABILITY=PASS
```

- [ ] **Step 5: Run full first-party schema tests**

```bash
.venv/bin/python -m pytest \
  tests/domains/test_domain_knowledge_package_first_party.py \
  -q
```

Required:

```text
PASS
```

- [ ] **Step 6: Verify differentiation did not regress**

Use the existing normalized policy-body test.

Required:

```text
DOMAIN_SPECIFIC_FIRST_PARTY_POLICIES=PASS
UNIQUE_FIRST_PARTY_POLICY_SHAPES>=4
```

Prefer preserving the V2 result:

```text
UNIQUE_FIRST_PARTY_POLICY_SHAPES=12
```

if the two corrected schemas remain naturally distinct.

Do not add meaningless policy entries solely to keep the count at 12.

- [ ] **Step 7: Re-run General observation-only compatibility**

Run its exact node from `AT-DP-049`.

Required:

```text
PASS
```

- [ ] **Step 8: Re-run resolved contradiction preservation**

Run the focused parameterized validation node and the connected acceptance node.

Required:

```text
PASS
```

- [ ] **Step 9: Ruff/format/diff-check the narrow production delta**

```bash
.venv/bin/ruff check \
  cmm/domains/concerns/knowledge_package.py \
  cmm/domains/life_plan/knowledge_package.py \
  tests/domains/test_domain_knowledge_package_first_party.py

.venv/bin/ruff format --check \
  cmm/domains/concerns/knowledge_package.py \
  cmm/domains/life_plan/knowledge_package.py \
  tests/domains/test_domain_knowledge_package_first_party.py

git diff --check
```

Required: PASS.

- [ ] **Step 10: Commit MAJOR-03 production remediation**

```bash
git add \
  cmm/domains/concerns/knowledge_package.py \
  cmm/domains/life_plan/knowledge_package.py

git diff --cached --check

git commit -m \
  "fix(domains): remove unreachable first party requirements"
```

Do not include tests already committed in Task 1 or documentation.

---

### Task 3: Strengthen Connected AT-DP-049 for Concerns and Life Plan Reachability

**Files:**
- Modify: `tests/domains/test_domain_knowledge_package_dp049_acceptance.py`
- Test: `tests/domains/test_domain_knowledge_package_first_party.py`

**Interfaces:**
- Consumes: corrected real Concerns/Life Plan schemas and canonical Phase 8 construction path.
- Produces: connected proof that the affected first-party schemas are usable through the canonical path.

- [ ] **Step 1: Reuse official existing in-memory fixtures**

Reuse the same canonical in-memory Knowledge Store/resources/registries already present in `AT-DP-049` or Phase 10.40 acceptance.

Do not introduce mock-only package constructors for the connected assertions.

- [ ] **Step 2: Add real Life Plan canonical-path acceptance**

Use the actual Life Plan `DomainDefinition` and its real schema.

Produce the package through:

```text
canonical KnowledgePackageBuilder
```

or the existing Domain→Cognitive seam if already used by the acceptance fixture.

Assert:

```python
assert result.knowledge_package is not None
assert type(result.knowledge_package) is KnowledgePackage
assert validate_domain_knowledge_package(
    result.knowledge_package,
    LIFE_PLAN_DOMAIN_DEFINITION.knowledge_package_schema,
) is result.knowledge_package
```

The exact definition constant name must be discovered from the repository.

Do not manually inject `active_goals` merely to make the test pass.

- [ ] **Step 3: Add real Concerns canonical-path acceptance**

Use the actual Concerns definition/schema through `DefaultDomainCognitiveIntegrator` when the current fixture can support it.

Do not manually pass `missing_information`.

Assert that the real built package validates successfully under the real Concerns schema.

- [ ] **Step 4: Add explicit canonical reachability evidence**

The acceptance should state via assertions/comments that:

```text
Life Plan does not require builder-unreachable active_goals
Concerns does not require integration-unreachable missing_information
```

The generalized invariant remains in the first-party test; AT-DP-049 proves the real connected path.

- [ ] **Step 5: Preserve distinct-policy composition scenario**

Retain the V2 composition scenario using genuinely different first-party schemas.

If it previously relied specifically on `active_goals.required_non_empty=True`, update the expected effective restriction to another real reachable Life Plan policy.

Do not weaken composition semantics.

- [ ] **Step 6: Preserve resolved contradiction acceptance**

Keep the V2 connected resolved-contradiction case unchanged unless fixture refactoring is necessary.

- [ ] **Step 7: Preserve authority-boundary assertions**

Retain all existing proof that schemas do not:

```text
execute validator refs
grant permissions
lower sensitivity
select provider/model
mutate stores
replace canonical package/builder
```

- [ ] **Step 8: Run AT-DP-049**

```bash
.venv/bin/python -m pytest \
  tests/domains/test_domain_knowledge_package_dp049_acceptance.py \
  -q
```

Required: PASS.

- [ ] **Step 9: Run focused remediation evidence together**

```bash
.venv/bin/python -m pytest \
  tests/domains/test_domain_knowledge_package_first_party.py \
  tests/domains/test_domain_knowledge_package_validation.py \
  tests/domains/test_domain_knowledge_package_dp049_acceptance.py \
  -q
```

Required: PASS.

- [ ] **Step 10: Commit strengthened acceptance**

```bash
git add \
  tests/domains/test_domain_knowledge_package_dp049_acceptance.py

git diff --cached --check

git commit -m \
  "test(domains): strengthen dp049 required field reachability"
```

If Task 3 requires a tiny change to the Task 1 test helper for correct canonical reachability logic, include only that exact test file as well and explain it in the commit body.

---

### Task 4: Correct the Three Documentation/Evidence Findings and Freeze One Coherent V3 Status

**Files:**
- Modify: `docs/reference/domain-knowledge-packages.md`
- Modify: `docs/roadmap/phase-10-domain-intelligence.md`
- Modify: `docs/reference/domain-intelligence-requirements-matrix.md`
- Modify: `ROADMAP.md`
- Modify conditionally: `docs/audits/domain-prompt-clause-coverage.md`
- Modify conditionally: `tests/domains/test_domain_prompt_clause_coverage.py`

**Interfaces:**
- Consumes: corrected code/tests and later Task 5 final gate evidence.
- Produces: accurate V3 pre-audit documentation without premature audit claims.

This task has two passes: semantic corrections now, exact gate-count freeze after Task 5.

- [ ] **Step 1: Correct MINOR-01 Languages rationale**

In `docs/reference/domain-knowledge-packages.md`, locate the Languages row.

Current semantic relationship:

```text
minimum_sensitivity=INTERNAL
memory_policy.sensitivity_limit=PERSONAL
```

Correct the rationale to state:

```text
Schema floor is less restrictive than memory sensitivity limit.
```

or equivalent wording.

Do not change the actual sensitivity values solely for documentation consistency.

- [ ] **Step 2: Correct MINOR-03 integration order**

Reference text must state:

```text
canonical resource adaptation
→ KnowledgePackageBuilder.build(...)
→ validate_domain_knowledge_package(...)
→ canonical CognitiveValidator
→ downstream cognition
```

Explicitly say Domain schema validation occurs:

```text
after canonical package construction
before canonical CognitiveValidator validation
```

Do not change production order.

- [ ] **Step 3: Remove premature severity-zero live claims**

From current live Phase 10.49 status sections, remove pre-audit claims such as:

```text
BLOCKERS=0
MAJORS=0
MINORS=0
```

unless clearly quoted as historical completed audit evidence with an explicit version label.

Current V3 pre-audit status must not imply independent V3 audit has happened.

- [ ] **Step 4: Unify live status to V3**

In all four live surfaces set current Phase 10.49 status to:

```text
PHASE10_49=IMPLEMENTED_PENDING_INDEPENDENT_REAUDIT_V3
```

Use remediation evidence:

```text
MAJOR_01=VERIFIED_REMEDIATED_IN_V2
MAJOR_02=VERIFIED_REMEDIATED_IN_V2
MAJOR_03=REMEDIATED_REPORTED
MINOR_01=REMEDIATED_REPORTED
MINOR_02=REMEDIATED_REPORTED
MINOR_03=REMEDIATED_REPORTED
FIRST_PARTY_REQUIRED_FIELD_REACHABILITY=PASS
DOMAIN_SPECIFIC_FIRST_PARTY_POLICIES=PASS
NON_FACT_CANONICAL_PACKAGE_COMPATIBILITY=PASS_WHERE_DOMAIN_POLICY_ALLOWS
RESOLVED_CONTRADICTION_PRESERVATION=PASS
DP-049=IMPLEMENTED_PENDING_INDEPENDENT_VERIFICATION
AT-DP-049=PASS_REPORTED
CLOSURE_ELIGIBLE=NO
```

- [ ] **Step 5: Verify there is no stale current-state marker**

Search live docs only:

```bash
git grep -n \
  'PHASE10_49=IMPLEMENTED_PENDING_INDEPENDENT_AUDIT\|PHASE10_49=IMPLEMENTED_PENDING_INDEPENDENT_REAUDIT_V2' \
  -- \
  ROADMAP.md \
  docs/roadmap/phase-10-domain-intelligence.md \
  docs/reference/domain-intelligence-requirements-matrix.md \
  docs/reference/domain-knowledge-packages.md
```

Expected: no stale **current** status. Historical context may be retained only when explicitly version-qualified and not presented as current state.

- [ ] **Step 6: Verify no premature closure**

```bash
if git grep -n 'PHASE10_49=CLOSED' -- \
  ROADMAP.md \
  docs/roadmap/phase-10-domain-intelligence.md \
  docs/reference/domain-intelligence-requirements-matrix.md \
  docs/reference/domain-knowledge-packages.md
then
  exit 1
fi
```

Expected: no match.

- [ ] **Step 7: Do not freeze test counts yet**

At this point, do not copy any old V1/V2 suite numbers into V3 evidence.

Use a temporary wording such as:

```text
V3_FINAL_GATE_COUNTS=PENDING_FINAL_EXACT_HEAD_RUN
```

only if the live docs require a placeholder-like state before Task 5. Prefer omitting counters until the final gate run rather than adding a temporary marker.

- [ ] **Step 8: Update clause coverage only if structurally necessary**

If `R10-C49`, `DP-049`, and clause mapping do not change, leave the ledger unchanged.

If the matrix status wording is fixture-checked, update only the exact test/fixture required.

- [ ] **Step 9: Run documentation semantics checks**

```bash
git grep -n \
  'PHASE10_49=IMPLEMENTED_PENDING_INDEPENDENT_REAUDIT_V3' \
  -- \
  ROADMAP.md \
  docs/roadmap/phase-10-domain-intelligence.md \
  docs/reference/domain-intelligence-requirements-matrix.md \
  docs/reference/domain-knowledge-packages.md

git grep -n \
  'MAJOR_03=REMEDIATED_REPORTED\|MINOR_01=REMEDIATED_REPORTED\|MINOR_02=REMEDIATED_REPORTED\|MINOR_03=REMEDIATED_REPORTED' \
  -- \
  ROADMAP.md \
  docs/roadmap/phase-10-domain-intelligence.md \
  docs/reference/domain-intelligence-requirements-matrix.md \
  docs/reference/domain-knowledge-packages.md

git diff --check
```

- [ ] **Step 10: Commit semantic documentation remediation**

```bash
git add \
  ROADMAP.md \
  docs/roadmap/phase-10-domain-intelligence.md \
  docs/reference/domain-intelligence-requirements-matrix.md \
  docs/reference/domain-knowledge-packages.md

git add \
  docs/audits/domain-prompt-clause-coverage.md \
  tests/domains/test_domain_prompt_clause_coverage.py \
  2>/dev/null || true

git diff --cached --check

git commit -m \
  "docs(domains): record phase 10.49 v2 remediation"
```

Stage conditional clause files only if actually changed.

---

### Task 5: Run the Complete V3 Gate Set, Freeze Exact Evidence, and Generate the Exact-HEAD V3 Bundle

**Files:**
- Modify: live Phase 10.49 docs only to record final authoritative counts after all code/test gates pass.
- No additional production changes unless a real remediation-caused regression is found.
- Output artifact: `phase-10.49-reaudit-v3-<12-char-head>.tar.gz`.

**Interfaces:**
- Consumes: completed V2→V3 remediation.
- Produces: one coherent exact-HEAD V3 candidate for independent ChatGPT re-audit.

- [ ] **Step 1: Run complete focused Phase 10.49 suite**

```bash
.venv/bin/python -m pytest \
  tests/domains/test_domain_knowledge_package_contracts.py \
  tests/domains/test_domain_knowledge_package_pack_integration.py \
  tests/domains/test_domain_knowledge_package_composition.py \
  tests/domains/test_domain_knowledge_package_validation.py \
  tests/domains/test_domain_knowledge_package_cognitive_integration.py \
  tests/domains/test_domain_knowledge_package_first_party.py \
  tests/domains/test_domain_knowledge_package_architecture.py \
  tests/domains/test_domain_knowledge_package_dp049_acceptance.py \
  -q
```

Required: PASS.

Record exact passed count.

- [ ] **Step 2: Run explicit V3 remediation evidence**

Run the exact node IDs for:

```text
test_all_first_party_required_non_empty_fields_are_canonically_reachable
test_first_party_knowledge_package_policies_are_meaningfully_domain_specific
General observation-only AT case
resolved contradiction preservation
Life Plan canonical-path AT case
Concerns canonical-path AT case
```

Required markers:

```text
FIRST_PARTY_REQUIRED_FIELD_REACHABILITY=PASS
DOMAIN_SPECIFIC_FIRST_PARTY_POLICIES=PASS
NON_FACT_CANONICAL_PACKAGE_COMPATIBILITY=PASS_WHERE_DOMAIN_POLICY_ALLOWS
RESOLVED_CONTRADICTION_PRESERVATION=PASS
AT-DP-049=PASS_REPORTED
```

- [ ] **Step 3: Run Phase 8 Knowledge Package and contradiction regressions**

```bash
.venv/bin/python -m pytest \
  tests/cognitive/test_knowledge_packages.py \
  tests/cognitive/test_cognitive_validation.py \
  -q
```

Discover and include any dedicated contradiction/privacy/resource test files semantically touched.

- [ ] **Step 4: Run Phase 10.39 fragmentation regressions**

Discover and run all Domain tests matching:

```text
*fragmentation*
*architecture_guard*
```

Required: PASS.

- [ ] **Step 5: Run Phase 10.40 Cognitive integration regressions**

Discover and run all:

```text
*cognitive_integration*
*dp040*
```

Required: PASS.

- [ ] **Step 6: Run Phase 10.44 memory/knowledge regressions**

Discover and run all:

```text
*memory_knowledge*
*dp044*
```

Required: PASS.

- [ ] **Step 7: Run Phase 10.46–10.48 regressions**

Discover and run all:

```text
*model_policy*
*benchmark*
*quality*
```

plus DP-046, DP-047, DP-048 connected acceptance tests.

Required: PASS.

- [ ] **Step 8: Run Domain Pack and SDK regressions**

At minimum:

```bash
.venv/bin/python -m pytest \
  tests/domains/test_domain_pack.py \
  tests/domains/test_domain_pack_serialization.py \
  tests/domains/test_domain_sdk_packager.py \
  -q
```

Include loader/manifest tests if current repository patterns require them.

Required: PASS.

- [ ] **Step 9: Run full Domain suite**

```bash
.venv/bin/python -m pytest tests/domains -q
```

Required: PASS.

If the local workspace harness reproduces the known filesystem broker artifact, use one clean exact-HEAD clone/export methodology already proven in prior Phase 10.49 work.

Do not mix failed sandbox-attempt counts into the final authoritative evidence.

- [ ] **Step 10: Run canonical global suite**

Use the repository-standard global pytest command.

Required: genuine PASS.

If a clean clone is required for authoritative execution, use that same final methodology consistently.

Record the one authoritative exact passed count.

- [ ] **Step 11: Run baseline-aware Ruff/format gate**

```bash
.venv/bin/python \
  scripts/audit/verify_phase_10_49_ruff_baseline.py
```

Required:

```text
CHANGED_PYTHON_RUFF_VIOLATIONS=0
CHANGED_PYTHON_FORMAT_FILES=0
NO_NEW_RUFF_REGRESSIONS=PASS
NO_NEW_FORMAT_REGRESSIONS=PASS
BASELINE_AWARE_GATE=PASS
```

Record every machine-readable value emitted by the gate.

Do not alter its baseline HEAD.

- [ ] **Step 12: Run compileall**

```bash
.venv/bin/python -m compileall -q \
  cmm cmm_agent kernel tests
```

Required: PASS.

- [ ] **Step 13: Run clause coverage**

```bash
.venv/bin/python -m pytest \
  tests/domains/test_domain_prompt_clause_coverage.py \
  -q
```

Required:

```text
PASS
UNCLASSIFIED_CLAUSES=0
DUPLICATE_PRIMARY_MAPPINGS=0
```

- [ ] **Step 14: Run diff/repository hygiene**

```bash
git diff --check
git status --short
```

All code/test/docs remediation must already be committed before the final evidence freeze.

- [ ] **Step 15: Remediate only if a real Phase 10.49 regression appears**

If a gate fails for a real product reason:

```text
reproduce
→ identify root cause
→ add/fix focused test
→ minimal correction
→ rerun affected gates
→ rerun complete focused Phase 10.49 suite
```

Commit only exact regression files:

```bash
git commit -m \
  "fix(domains): remediate phase 10.49 v3 gate regression"
```

Do not clean unrelated historical debt.

- [ ] **Step 16: Freeze one coherent authoritative gate set in live docs**

Update every live Phase 10.49 surface that carries counts with the same exact final numbers from the final authoritative runs.

Use:

```text
PHASE10_49=IMPLEMENTED_PENDING_INDEPENDENT_REAUDIT_V3

MAJOR_01=VERIFIED_REMEDIATED_IN_V2
MAJOR_02=VERIFIED_REMEDIATED_IN_V2
MAJOR_03=REMEDIATED_REPORTED
MINOR_01=REMEDIATED_REPORTED
MINOR_02=REMEDIATED_REPORTED
MINOR_03=REMEDIATED_REPORTED

FIRST_PARTY_REQUIRED_FIELD_REACHABILITY=PASS
DOMAIN_SPECIFIC_FIRST_PARTY_POLICIES=PASS
NON_FACT_CANONICAL_PACKAGE_COMPATIBILITY=PASS_WHERE_DOMAIN_POLICY_ALLOWS
RESOLVED_CONTRADICTION_PRESERVATION=PASS

DP-049=IMPLEMENTED_PENDING_INDEPENDENT_VERIFICATION
AT-DP-049=PASS_REPORTED
CLOSURE_ELIGIBLE=NO
```

Do not include live pre-audit zero severity counts.

- [ ] **Step 17: Verify final live-doc consistency mechanically**

Use a small Python script or shell extraction to read all four canonical live docs and verify they report the same:

```text
phase status
focused count
Domain-suite count
global-suite count
Ruff/format result
compileall result
clause result
reachability result
AT-DP-049 result
```

If a count is intentionally present in only one reference document, ensure no conflicting value exists elsewhere.

Required:

```text
LIVE_STATUS_CONSISTENCY=PASS
LIVE_GATE_EVIDENCE_CONSISTENCY=PASS
```

- [ ] **Step 18: Commit final V3 pre-audit evidence**

```bash
git add \
  ROADMAP.md \
  docs/roadmap/phase-10-domain-intelligence.md \
  docs/reference/domain-intelligence-requirements-matrix.md \
  docs/reference/domain-knowledge-packages.md

git diff --cached --check

git commit -m \
  "docs(domains): record phase 10.49 v3 preaudit evidence"
```

Do not create an empty commit.

- [ ] **Step 19: Capture exact V3 implementation HEAD**

```bash
V3_IMPLEMENTATION_HEAD="$(git rev-parse HEAD)"
SHORT_HEAD="${V3_IMPLEMENTATION_HEAD:0:12}"

test -z "$(git status --porcelain)"
git diff --check

git stash list | grep -F \
  "quarantine: post-audit phase 10.32 uncommitted changes"

echo "V3_IMPLEMENTATION_HEAD=$V3_IMPLEMENTATION_HEAD"
echo "WORKTREE=CLEAN"
echo "QUARANTINE_STASH=PRESERVED"
```

- [ ] **Step 20: Verify historical V1 and V2 bundles remain unchanged**

```bash
DL="$HOME/Library/Mobile Documents/com~apple~CloudDocs/Downloads"

V1="$DL/phase-10.49-audit-80f9f77a805f.tar.gz"
V2="$DL/phase-10.49-reaudit-v2-565ebb72216c.tar.gz"

test -f "$V1"
test -f "$V2"

test "$(
  shasum -a 256 "$V1" |
  awk '{print $1}'
)" = "fc26827abb373337377197a6485a10416daa07b4abb088366cc111019020eead"

test "$(
  shasum -a 256 "$V2" |
  awk '{print $1}'
)" = "dca02b2ca06324a919e95a60aa0bea3f19f242eae661a549aca749535ba85ff5"

echo "V1_BUNDLE=UNCHANGED"
echo "V2_BUNDLE=UNCHANGED"
```

- [ ] **Step 21: Generate the new exact-HEAD V3 bundle**

```bash
OUT="$DL/phase-10.49-reaudit-v3-${SHORT_HEAD}.tar.gz"

test ! -e "$OUT"

git archive \
  --format=tar.gz \
  --prefix=CMM-OS/ \
  -o "$OUT" \
  "$V3_IMPLEMENTATION_HEAD"
```

Do not tar the worktree manually.

- [ ] **Step 22: Verify V3 bundle integrity and exact commit**

```bash
gzip -t "$OUT"

ARCHIVE_COMMIT_ID="$(
  git get-tar-commit-id < <(gzip -dc "$OUT")
)"

test "$ARCHIVE_COMMIT_ID" = "$V3_IMPLEMENTATION_HEAD"

V3_BUNDLE_SHA256="$(
  shasum -a 256 "$OUT" |
  awk '{print $1}'
)"

echo "REAUDIT_V3_IMPLEMENTATION_HEAD=$V3_IMPLEMENTATION_HEAD"
echo "REAUDIT_V3_BUNDLE=$OUT"
echo "REAUDIT_V3_BUNDLE_SHA256=$V3_BUNDLE_SHA256"
echo "REAUDIT_V3_ARCHIVE_COMMIT_ID=$ARCHIVE_COMMIT_ID"
```

- [ ] **Step 23: Final no-mutation verification**

```bash
test -z "$(git status --porcelain)"
git diff --check

git stash list | grep -F \
  "quarantine: post-audit phase 10.32 uncommitted changes"

echo "WORKTREE=CLEAN"
echo "QUARANTINE_STASH=PRESERVED"
echo "PUSH=NO"
echo "MERGE=NO"
```

- [ ] **Step 24: Stop for independent ChatGPT Re-audit V3**

Final agent handoff must include:

```text
PHASE10_49=IMPLEMENTED_PENDING_INDEPENDENT_REAUDIT_V3

STARTING_HEAD=73d2e4501349194b038a4cb734c178baab1ec6b0
V2_AUDITED_IMPLEMENTATION_HEAD=565ebb72216ce288e6c05fd6d666667b19b8792a
V3_IMPLEMENTATION_HEAD=<full SHA>

MAJOR_01=VERIFIED_REMEDIATED_IN_V2
MAJOR_02=VERIFIED_REMEDIATED_IN_V2
MAJOR_03=REMEDIATED_REPORTED
MINOR_01=REMEDIATED_REPORTED
MINOR_02=REMEDIATED_REPORTED
MINOR_03=REMEDIATED_REPORTED

FIRST_PARTY_REQUIRED_FIELD_REACHABILITY=PASS
DOMAIN_SPECIFIC_FIRST_PARTY_POLICIES=PASS
UNIQUE_FIRST_PARTY_POLICY_SHAPES=<exact>
NON_FACT_CANONICAL_PACKAGE_COMPATIBILITY=PASS_WHERE_DOMAIN_POLICY_ALLOWS
RESOLVED_CONTRADICTION_PRESERVATION=PASS

FIRST_PARTY_SCHEMAS=12
MENTAL_HEALTH_SCHEMA=NOT_IMPLEMENTED
NEURODIVERGENCE_SCHEMA=NOT_IMPLEMENTED

CANONICAL_KNOWLEDGE_PACKAGE=cmm.cognitive.knowledge_packages.KnowledgePackage
CANONICAL_KNOWLEDGE_PACKAGE_BUILDER=cmm.cognitive.knowledge_packages.KnowledgePackageBuilder

PARALLEL_BUILDER=NONE
PARALLEL_REGISTRY=NONE
PARALLEL_LOADER=NONE
PARALLEL_RESOLVER=NONE
PARALLEL_STORE=NONE
PARALLEL_RUNTIME=NONE
PARALLEL_ENGINE=NONE

DP-049=IMPLEMENTED_PENDING_INDEPENDENT_VERIFICATION
AT-DP-049=PASS_REPORTED
CLOSURE_ELIGIBLE=NO

RED_MAJOR_03=<exact node + failure>
FOCUSED_PHASE10_49=<exact count>
REMEDIATION_EVIDENCE_TESTS=<exact count>
PHASE8_REGRESSIONS=<exact count>
PHASE10_39_REGRESSIONS=<exact count>
PHASE10_40_REGRESSIONS=<exact count>
PHASE10_44_REGRESSIONS=<exact count>
PHASE10_46_REGRESSIONS=<exact count>
PHASE10_47_REGRESSIONS=<exact count>
PHASE10_48_REGRESSIONS=<exact count>
DOMAIN_PACK_REGRESSIONS=<exact count>
DOMAIN_SDK_REGRESSIONS=<exact count>
DOMAIN_SUITE=<exact count>
GLOBAL_SUITE=<exact count>

RUFF_VERSION=<exact>
BASELINE_HEAD=<exact>
GLOBAL_RUFF_BASELINE=<exact>
GLOBAL_RUFF_CURRENT=<exact>
GLOBAL_FORMAT_BASELINE=<exact>
GLOBAL_FORMAT_CURRENT=<exact>
CHANGED_PYTHON_FILES=<exact>
CHANGED_PYTHON_RUFF_VIOLATIONS=0
CHANGED_PYTHON_FORMAT_FILES=0
NO_NEW_RUFF_REGRESSIONS=PASS
NO_NEW_FORMAT_REGRESSIONS=PASS
BASELINE_AWARE_GATE=PASS

COMPILEALL=PASS
CLAUSE_COVERAGE=PASS
UNCLASSIFIED_CLAUSES=0
DUPLICATE_PRIMARY_MAPPINGS=0
GIT_DIFF_CHECK=PASS
LIVE_STATUS_CONSISTENCY=PASS
LIVE_GATE_EVIDENCE_CONSISTENCY=PASS

V1_BUNDLE_SHA256=fc26827abb373337377197a6485a10416daa07b4abb088366cc111019020eead
V2_BUNDLE_SHA256=dca02b2ca06324a919e95a60aa0bea3f19f242eae661a549aca749535ba85ff5
REAUDIT_V3_BUNDLE=<absolute path>
REAUDIT_V3_BUNDLE_SHA256=<sha256>
REAUDIT_V3_ARCHIVE_COMMIT_ID=<full SHA>

WORKTREE=CLEAN
QUARANTINE_STASH=PRESERVED
PUSH=NO
MERGE=NO
NEXT=INDEPENDENT_CHATGPT_REAUDIT_V3
```

Also list every remediation commit in chronological order.

Do not self-audit.

Do not close Phase 10.49.

---

## Final V3 Remediation Acceptance Checklist

Before handing off the V3 bundle:

```text
V2_REAUDIT=FAIL_RECORDED

MAJOR_01=VERIFIED_REMEDIATED_IN_V2
MAJOR_02=VERIFIED_REMEDIATED_IN_V2
MAJOR_03=REMEDIATED_REPORTED

MINOR_01=REMEDIATED_REPORTED
MINOR_02=REMEDIATED_REPORTED
MINOR_03=REMEDIATED_REPORTED

FIRST_PARTY_REQUIRED_FIELD_REACHABILITY=PASS
DOMAIN_SPECIFIC_FIRST_PARTY_POLICIES=PASS
UNIQUE_FIRST_PARTY_POLICY_SHAPES>=4
NON_FACT_CANONICAL_PACKAGE_COMPATIBILITY=PASS_WHERE_DOMAIN_POLICY_ALLOWS
RESOLVED_CONTRADICTION_PRESERVATION=PASS

FIRST_PARTY_SCHEMAS=12
MENTAL_HEALTH_SCHEMA=NOT_IMPLEMENTED
NEURODIVERGENCE_SCHEMA=NOT_IMPLEMENTED

CANONICAL_KNOWLEDGE_PACKAGE=KnowledgePackage
CANONICAL_BUILDER=KnowledgePackageBuilder

PARALLEL_BUILDER=NONE
PARALLEL_REGISTRY=NONE
PARALLEL_LOADER=NONE
PARALLEL_RESOLVER=NONE
PARALLEL_STORE=NONE
PARALLEL_RUNTIME=NONE
PARALLEL_ENGINE=NONE

AT-DP-049=PASS_REPORTED
DP-049=IMPLEMENTED_PENDING_INDEPENDENT_VERIFICATION

FOCUSED_TESTS=PASS
PHASE8_REGRESSIONS=PASS
PHASE10_39_REGRESSIONS=PASS
PHASE10_40_REGRESSIONS=PASS
PHASE10_44_REGRESSIONS=PASS
PHASE10_46_REGRESSIONS=PASS
PHASE10_47_REGRESSIONS=PASS
PHASE10_48_REGRESSIONS=PASS
DOMAIN_PACK_REGRESSIONS=PASS
DOMAIN_SDK_REGRESSIONS=PASS
DOMAIN_SUITE=PASS
GLOBAL_SUITE=PASS
BASELINE_AWARE_RUFF_FORMAT=PASS
COMPILEALL=PASS
CLAUSE_COVERAGE=PASS
GIT_DIFF_CHECK=PASS
LIVE_STATUS_CONSISTENCY=PASS
LIVE_GATE_EVIDENCE_CONSISTENCY=PASS

PHASE10_49=IMPLEMENTED_PENDING_INDEPENDENT_REAUDIT_V3
CLOSURE_ELIGIBLE=NO

WORKTREE=CLEAN
QUARANTINE_STASH=PRESERVED
V1_BUNDLE=UNCHANGED
V2_BUNDLE=UNCHANGED
V3_BUNDLE=EXACT_COMMITTED_HEAD
PUSH=NO
MERGE=NO
```

Independent Re-audit V3, not this plan, decides whether:

```text
BLOCKERS=0
MAJORS=0
MINORS=0
MAJOR_03=VERIFIED_REMEDIATED
MINOR_01=VERIFIED_REMEDIATED
MINOR_02=VERIFIED_REMEDIATED
MINOR_03=VERIFIED_REMEDIATED
DP-049=VERIFIED_EXISTING
AT-DP-049=PASS
CLOSURE_ELIGIBLE=YES
```
